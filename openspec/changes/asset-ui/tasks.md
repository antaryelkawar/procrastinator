# Tasks: asset-ui

All tasks are `[ui]` (Vite react-ts + Tailwind v4 + shadcn/ui + React Router + TanStack Query + react-dropzone). Each task lists the files it **owns**; orchestrator batches on disjoint ownership. Every task ships its tests in the same task and is done when `npm run test` and `npm run build` pass in `ui/`.

## 1. Scaffold and tooling

- [x] 1.1 [ui] Scaffold the Vite react-ts app in `ui/` with Tailwind v4 (`@tailwindcss/vite`), Vitest + React Testing Library + vitest-axe test setup, and dev proxy `/api` → `http://localhost:8080`. Owns: `ui/package.json`, `ui/vite.config.ts`, `ui/tsconfig.json`, `ui/tsconfig.node.json`, `ui/index.html`, `ui/.gitignore`, `ui/src/main.tsx`, `ui/src/index.css`, `ui/src/test/setup.ts`. Verify: `npm install`, `npm run build`, and `npm run test` all succeed; proxy block present in `vite.config.ts`.
- [x] 1.2 [ui] Init shadcn/ui and add the full component set (`button card table input select label textarea dialog alert-dialog badge progress sheet skeleton separator dropdown-menu sonner`) plus the `cn()` helper. Owns: `ui/components.json`, `ui/src/components/ui/**`, `ui/src/lib/utils.ts`. Verify: smoke test renders Button, Card, Table, Dialog without errors; axe-clean.

## 2. Core libraries

- [x] 2.1 [ui] API types + client + error mapping per design D1/D5: DTO interfaces mirroring the backend JSON (omitted-when-null optionals, `lines: null` in batch lists, flat link fields), `apiFetch` with `userPath`/`financePath` + `X-Tenant-ID` injection for finance routes, `ApiError { status, message }`, status→copy mapping (400/404/409/413/415/422/502/network). Owns: `ui/src/lib/api/types.ts`, `ui/src/lib/api/client.ts`, `ui/src/lib/api/errors.ts`, `ui/src/lib/api/client.test.ts`, `ui/src/lib/api/errors.test.ts`. Verify: stubbed-fetch tests assert correct URLs for documents/assets vs finance, header injection, error-envelope parsing, and every status mapping.
- [x] 2.2 [ui] Exact money and date formatting per design D2/D3: `formatMoney` (string-only grouping + currency symbol map with ISO fallback), signed-amount display helper, `formatDate`/`formatTimestamp` (string surgery, no `new Date` on API values), `isWarrantyExpired`, `todayLocalISO`. Owns: `ui/src/lib/format/money.ts`, `ui/src/lib/format/date.ts`, `ui/src/lib/format/money.test.ts`, `ui/src/lib/format/date.test.ts`. Verify: `39999.99 INR` renders exactly (no float artifacts); `occurred_on "2026-08-20"` renders as 2026-08-20 regardless of TZ (test under shifted TZ); warranty expiry compares date strings.
- [x] 2.3 [ui] Active user context per design D4: `ActiveUserProvider` + `useActiveUser()`, localStorage persistence, `^[A-Za-z0-9_-]{1,64}$` validation, `queryClient.clear()` on switch. Owns: `ui/src/context/active-user.tsx`, `ui/src/context/active-user.test.tsx`. Verify: tests cover default user, format rejection, persistence, and that switching users clears the query cache.
- [x] 2.4 [ui] XHR multipart upload helper per design D6: `uploadDocument(user, file, onProgress)` and `uploadStatement(user, accountId, file, onProgress)` with fake-XHR tests driving progress events and all error statuses. Owns: `ui/src/lib/api/upload.ts`, `ui/src/lib/api/upload.test.ts`. Verify: tests assert multipart field names (`file`, `account_id`), progress callbacks, and `ApiError` rejection per status.

## 3. Data layer and app shell

- [x] 3.1 [ui] TanStack Query hooks per design D5: all seven queries (assets, asset, asset-documents, accounts, movements with `{accountId,from,to}` filter key, batches, batch) and eight mutations with the invalidation map (upload→assets; account→accounts; movement mutations→movements+accounts; batch mutations→batches+batch, commit also→movements+accounts). Owns: `ui/src/lib/api/hooks.ts`, `ui/src/lib/api/hooks.test.tsx`. Verify: tests (mocked client module) assert query keys include `userId` and each mutation invalidates exactly the specified prefixes.
- [x] 3.2 [ui] App shell, router, and shared feedback per design D7/D9: route table (`/`→`/assets`, `/upload`, `/assets`, `/assets/:assetId`, `/finance/accounts`, `/finance/movements`, `/finance/import`, `/finance/import/history`, `/finance/import/:batchId`), responsive shell (sidebar ≥md, hamburger+Sheet <md, always-visible active-user switcher), `Loading`/`ErrorState(retry)`/`EmptyState` components, shared `ConfirmDialog` over alert-dialog. Owns: `ui/src/router.tsx`, `ui/src/components/layout/app-shell.tsx`, `ui/src/components/feedback/loading.tsx`, `ui/src/components/feedback/error-state.tsx`, `ui/src/components/feedback/empty-state.tsx`, `ui/src/components/confirm-dialog.tsx`, `ui/src/components/layout/app-shell.test.tsx`. Verify: tests cover nav to all six screens without reload, deep-link render of `/assets/:id`, mobile nav reachable via sheet, confirm dialog confirm/cancel; axe-clean.

## 4. Screens

- [x] 4.1 [ui] Document upload screen: react-dropzone (PDF/PNG/JPEG, drag-drop + click), per-file state machine (queued→uploading→success(asset identity)|error(mapped reason)) with live XHR progress, concurrency 3, per-file retry on 502/network, failures isolated, invalidates `['assets',uid]` on success. Owns: `ui/src/pages/upload/upload-page.tsx`, `ui/src/pages/upload/upload-page.test.tsx`. Verify: tests cover progress→success (201 with asset), 415 reason, 502 retryable failure, one failure not blocking others; axe-clean.
- [x] 4.2 [ui] Asset list screen: table ≥md / cards <md showing brand, model, serial number, warranty end; expired warranty visually marked (string date comparison); document-type badge; empty state with upload call-to-action; row navigates to detail. Owns: `ui/src/pages/assets/asset-list-page.tsx`, `ui/src/pages/assets/asset-list-page.test.tsx`. Verify: tests cover key-field rendering, expired-warranty marking, empty state, navigation, loading/error states; axe-clean.
- [x] 4.3 [ui] Asset detail screen: populated asset fields with exact price+currency rendering, documents list (type, source filename, upload timestamp), 404 not-found state with link back to list. Owns: `ui/src/pages/assets/asset-detail-page.tsx`, `ui/src/pages/assets/asset-detail-page.test.tsx`. Verify: tests cover fields+documents render, `39999.99 INR` exactness, unknown-id not-found; axe-clean.
- [x] 4.4 [ui] Finance accounts screen: account list with name/type/currency/derived balance (string, as returned), create-account form (name, type from {bank,wallet,cash,credit_card}, currency, optional institution/descriptor) with client-side validation (blank name, missing type/currency) and 400 reason display retaining entered values. Owns: `ui/src/pages/finance/accounts-page.tsx`, `ui/src/pages/finance/accounts-page.test.tsx`. Verify: tests cover balance display (`250`−`100`=`150` as returned string), client-side rejection sends no request, server 400 shows reason and keeps values; axe-clean.
- [x] 4.5 [ui] Money movements screen (list + filters + row display): kind, signed amount with currency (D2), occurred date verbatim, description, origin badge (`manual`/`import`), account(s); account + `from`/`to` date-range filters wired to query params; imported movements show no delete affordance. Owns: `ui/src/pages/finance/movements-page.tsx`, `ui/src/pages/finance/movements-page.test.tsx`. Verify: tests assert filter params `account_id`/`from`/`to` reach the client, signed display per kind, import badge, no delete on imported rows; axe-clean.
- [x] 4.6 [ui] Movement creation form: kinds expense/income/transfer; positive decimal amount (string validation, never parseFloat), occurred date, non-empty description; kind-specific account selection (expense=source, income=destination, transfer=two distinct same-currency accounts); client-side rejection of non-positive amount, blank description, missing account, identical transfer accounts, and cross-currency transfer — all before any request; success resets the form and invalidates movements+accounts. Owns: `ui/src/pages/finance/movement-create-form.tsx`, `ui/src/pages/finance/movement-create-form.test.tsx`. Verify: tests cover every listed validation rule (incl. cross-currency) and post-success reset/invalidation; axe-clean.
- [x] 4.7 [ui] Movement edit-description and delete actions: description correction dialog (PATCH, any origin, blank rejected client-side) and manual-movement deletion behind `ConfirmDialog`; row shows corrected description with other fields unchanged. Owns: `ui/src/pages/finance/movement-actions.tsx`, `ui/src/pages/finance/movement-actions.test.tsx`; edits `ui/src/pages/finance/movements-page.tsx` to wire row actions. Verify: tests cover PATCH round-trip, blank rejection, delete confirm/cancel, no delete for imported; axe-clean.
- [x] 4.8 [ui] Movement–document linking: link dialog (choose document, POST `document_id`), unlink action, linked display (document type + filename + creator kind `manual`/`auto`), conflict flag rendering with both values shown unchanged, 409 surfaced as "document already linked" message. Owns: `ui/src/pages/finance/movement-link.tsx`, `ui/src/pages/finance/movement-link.test.tsx`; edits `ui/src/pages/finance/movements-page.tsx` to wire link display/actions. Verify: tests cover linked display, conflicting-link marking (40000 vs 39500 both shown), 409 message, unlink flow; axe-clean.
- [x] 4.9 [ui] Statement import screen: account select + CSV/PDF file upload (multipart `account_id`+`file`, progress via 2.4), preview render (filename, format, per-status counts, per-line date/signed-amount/description/status incl. error reason), commit/discard behind `ConfirmDialog`, commit summary (`created`/`skipped`) + invalidation per D5, mapped rejection reasons (400/404/413/415/422 incl. no-parseable-lines). Owns: `ui/src/pages/finance/import-page.tsx`, `ui/src/pages/finance/import-lines-table.tsx`, `ui/src/pages/finance/import-page.test.tsx`. Verify: tests cover per-line statuses (8 valid/1 duplicate/1 error), commit confirm→summary, discard confirm→discarded state with commit no longer offered, 422 scanned-PDF explanation; axe-clean.
- [x] 4.10 [ui] Import history + batch detail: history list (filename, account, state badge, per-status counts, created timestamp), detail view reusing `import-lines-table.tsx` with commit/discard offered only for `preview` batches, empty-history state. Owns: `ui/src/pages/finance/import-history-page.tsx`, `ui/src/pages/finance/import-history-page.test.tsx`. Verify: tests cover list rendering with states, preview batch actionable from detail, committed/discarded not actionable, empty state; axe-clean.

## 5. Final verification

- [ ] 5.1 [ui] End-to-end smoke against the live backend: `npm run dev` with backend on `:8080`; walk all six screens at desktop and 360px widths (no page-level horizontal scroll), switch active user and confirm data replacement, run one real document upload and one CSV statement import preview→commit. Owns: no files (verification only). Verify: browser-driven pass (ui-impl-worker tooling) with screenshots per screen; `npm run test` and `npm run build` green.
