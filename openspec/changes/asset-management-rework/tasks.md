# Tasks: asset-management-rework

Backend tasks are `[backend]` (Go, module `procrastinator-backend/`, flat
`api/ core/ infra/ commons/`). UI tasks are `[ui]` (Vite react-ts + Tailwind v4 +
shadcn/ui + React Router 7 + TanStack Query 5 + TanStack Table, in `ui/`). Each
task lists the files it **owns**; the orchestrator batches on disjoint ownership
(shared files â€” `config.go` — have a single canonical owner
(6.6); other tasks reference but do not edit them; `core/identity/resolve.go` is
owned by 3.1/3.2, re-owned by 10.10 (done, sequential); `api/server.go` is
re-owned by 10.12 for the api modularization (6.6 done, sequential)).
Every task ships its tests in the same task and is verifiable (a passing test, an
observable behavior, or an endpoint contract) â€” see `Verify:`.

Build/verify commands:
- Backend: `cd procrastinator-backend && go build ./... && go test ./...`
- Codegen (OpenAPI source-of-truth): from repo root `make codegen-go`,
  `make codegen-ts`, `make codegen-ts-client`, `make codegen-drift-check`
- UI: `cd ui && npm install && npm run test && npm run build`

Spec â†’ task traceability: each task names the capability/spec(s) it satisfies.

## 1. Data model & migration

- [x] 1.1 [backend] Add migration `procrastinator-backend/migrations/00006_asset_rework.sql`: on `assets` add `name text`, `norm_name text`, `asset_category text`, `category_confidence real`, `category_user_set boolean NOT NULL DEFAULT false`, `deleted_at timestamptz`, `merged_into uuid REFERENCES assets(id) ON DELETE SET NULL`, `merged_at timestamptz`, and drop `doc_type`; expand `documents.doc_type` and `ingest_reviews.doc_type` CHECKs from `{invoice,warranty,amc,other}` to `{invoice,receipt,warranty,amc,statement,other}`; make `documents.asset_id` nullable; add `ingest_reviews.provenance jsonb NOT NULL DEFAULT '{}'`; add indexes `idx_assets_owner_norm_brand_model (owner_id, owner_household_id, norm_brand, norm_model)` and `idx_assets_owner_norm_name_model (owner_id, owner_household_id, norm_name, norm_model)`. Provide a complete `-- +goose Down` reversing every step. Satisfies: asset-registry (canonical model), asset-taxonomy (category+confidence), asset-lifecycle (delete/merge), processing-pipeline + asset-identity-resolution (lookup indexes), processing-pipeline (provenance storage). Owns: `migrations/00006_asset_rework.sql`. Verify: `cd procrastinator-backend && go test ./infra/postgres/ -run 'TestMigrate|TestMigration' -count=1` applies 00006 up and down cleanly against the test schema (or the existing migrate test target); `psql`-style assertion that `assets` has the new columns and no `doc_type`; `ingest_reviews` has `provenance jsonb`.
- [x] 1.2 [backend] Extend the entity model: `commons/entity/asset.go` â€” add `Name *string`, `NormName *string`, `AssetCategory *string`, `CategoryConfidence *float64`, `CategoryUserSet bool`, `DeletedAt *time.Time`, `MergedInto *string`, `MergedAt *time.Time`, and remove `DocType`; add category vocabulary constants + `ValidAssetCategory` (`appliance, electronics, computing, furniture, vehicle, tool, clothing, document_only, other`); extend the document classification vocabulary to `{invoice,receipt,warranty,amc,statement,other}` + `ValidDocType` (move/expand the doc-type constants out of `asset.go` into a shared `commons/entity/doc_type.go`). `commons/entity/extraction.go` â€” add `Name *string`, `WarrantyDuration string`, `AssetCategory *string`. `commons/entity/review.go` â€” add `Provenance map[string]any` to `IngestReview` (maps to `ingest_reviews.provenance` JSONB). Update the postgres scan/marshal mapping for the new/removed asset columns + the review provenance column in `infra/postgres/repository.go` (or the asset/review row mapper) + `infra/postgres/repos.go`. Satisfies: asset-registry, asset-taxonomy, processing-pipeline (provenance storage). Owns: `commons/entity/asset.go`, `commons/entity/extraction.go`, `commons/entity/doc_type.go`, `commons/entity/review.go` (+ tests), `infra/postgres/repository.go` (asset + review row scan/marshal), `infra/postgres/repos.go`. Verify: `go test ./commons/entity/ ./infra/postgres/ -run 'Asset|Extraction|Review' -count=1` â€” a round-trip create/read preserves name/category/confidence and no doc_type; IngestReview round-trips provenance JSONB; `ValidAssetCategory`/`ValidDocType` unit tests cover in/out-of-vocabulary values.

## 2. Extraction building blocks

- [x] 2.1 [backend] Deterministic warranty arithmetic in `commons/dates.go`: add `AddWarrantyEnd(purchase *time.Time, duration string, explicitEnd *time.Time) (*time.Time, bool)` + a duration parser for `N years|months|days`, `N yr|mo|d`, and ISO-8601 (`P2Y`, `P1M`, `P2Y6M`). Explicit `warranty_end` takes precedence; unparseable duration â†’ `ok=false` (field absent). Satisfies: processing-pipeline (deterministic warranty duration), llm-extraction (structured field extraction). Owns: `commons/dates.go`, `commons/dates_test.go`. Verify: `go test ./commons/ -run Warranty -count=1` â€” table tests: `2026-01-15 + "2 years"` â†’ `2028-01-15`; `"24 months"` â†’ `2028-01-15`; explicit `2027-06-30` wins over `"2 years"`; `"sometime"` â†’ `ok=false`; ISO `P1Y` â†’ `+1y`.
- [x] 2.2 [backend] Parse the new extraction fields: in `commons/parse/parse.go`, parse `name`, `warranty_duration`, and `asset_category` from the raw LLM JSON using the existing permissive-absence rule (bad value â†’ absent, never an error); set them on `entity.Extraction`. Satisfies: llm-extraction (structured field extraction), asset-taxonomy. Owns: `commons/parse/parse.go`, `commons/parse/parse_test.go`. Verify: `go test ./commons/parse/ -count=1` â€” valid values populate the new fields; missing/malformed values leave them absent without dropping other valid fields.
- [x] 2.3 [backend] Known-brand lexicon + deterministic product-description splitter in `core/processing/`: `brands.go` (embedded canonical brand list â€” incl. Cooler Master, LG, Samsung, Whirlpool, IFB, Bosch; case-insensitive word-boundary longest-match; overridable via `PROCRASTINATOR_BRAND_LEXICON` JSON path) and `split.go` `SplitDescription(desc string, lex *BrandLexicon) (brand, name, model string)` (collapse+title-case, extract lexicon brand, trailing model-like token = `model`, remaining = `name`). Satisfies: processing-pipeline (brand/model recognition, product description splitting), asset-taxonomy (canonical name). Owns: `core/processing/brands.go`, `core/processing/split.go`, `core/processing/split_test.go`. Verify: `go test ./core/processing/ -run 'Split|Lexicon' -count=1` â€” `"MICRO WAVE OVEN CONVECTION 30BRC2"` â†’ name `Microwave Oven`, model `30BRC2`, brand ``; `"CABINET COOLER MASTER CD600 BLACK"` â†’ brand `Cooler Master`, name `Cabinet`, model `CD600`; the full raw string is never returned as `model`.
- [x] 2.4 [backend] Parallel multi-worker extraction + pure consensus in `core/processing/`: `extract.go` runs N `Worker{Model,BaseURL,Strategy}` via `errgroup` (each its own timeout), each issuing one OpenAI-compatible `/chat/completions` request through the existing `infra/llm.Client.Chat` and parsing via `parse.ParseExtraction`, recording per-worker errors (failure not fatal); `consensus.go` `Consensus(workers []WorkerResult) (entity.Extraction, float64, []string)` implementing D2 (s=0â†’failed; s=1â†’conf 0.6; sâ‰¥2 â†’ per-identity-field conflict â†’ unresolved+conf 0.5, else conf 0.9; non-identity fields = first non-nil; brand validated/corrected against the lexicon via 2.3; description-split reconciliation). Accept the worker list as a constructor parameter (config parsing lives in 6.6). Satisfies: processing-pipeline (parallel multi-model extraction), llm-extraction (parallel request, mandatory). Owns: `core/processing/extract.go`, `core/processing/consensus.go`, `core/processing/consensus_test.go`, `core/processing/extract_test.go`. Verify: `go test ./core/processing/ -run 'Consensus' -count=1` â€” two workers agree â†’ conf â‰¥ 0.8; one worker fails â†’ conf 0.6; serial disagree â†’ field nil + conf 0.5; all fail â†’ no canonical write (s=0); non-identity disagreement does not lower conf.
- [x] 2.5 [backend] Update the extraction prompt `infra/llm/prompt.go SystemPrompt()` to request: the split **brand / canonical name / model** (explicitly "do not put the whole description in model"), `asset_category` from the D5 vocabulary, and `warranty_duration` as written. Keep the OpenAI chat-completions shape. Satisfies: llm-extraction (structured field extraction, parallel requests per worker). Owns: `infra/llm/prompt.go`, `infra/llm/prompt_test.go`. Verify: `go test ./infra/llm/ -run Prompt -count=1` â€” the system prompt contains instructions for name/model/brand splitting, the category vocabulary, and `warranty_duration`; still a valid chat-completions system prompt.
- [x] 2.6 [backend] Category inference in `core/processing/category.go`: `InferCategory(name, brand string, desc string, llmCategory *string) (category string, confidence float64)` â€” prefer the LLM's in-vocabulary `asset_category`, else a deterministic keyword classifier on name/brand/description; confidence 0.9 (agree) / 0.6 (single source) / 0.5 (none â†’ `other`). Satisfies: asset-taxonomy (intrinsic category, inference), processing-pipeline. Owns: `core/processing/category.go`, `core/processing/category_test.go`. Verify: `go test ./core/processing/ -run Category -count=1` â€” microwave â†’ `appliance`; cabinet (Cooler Master) â†’ `furniture` or `computing`; LLM `appliance` + classifier `appliance` â†’ conf 0.9; no signal â†’ `other` conf 0.5.

## 3. Identity resolution

- [x] 3.1 [backend] Efficient 3-stage indexed lookup in `core/identity/resolve.go`: extend `Resolve`/`Match`/`lookup` to run stages in order (1) `norm_serial` (unique â†’ â‰¤1), (2) `norm_brand`+`norm_model`, (3) `norm_name`+`norm_model`, each `List(..., Limit(candidateLimit))` (default from `repo` option / passed-in limit), first stage with â‰¥1 candidate short-circuits; a non-serial stage with exactly 1 candidate â†’ merge, >1 â†’ return an `ambiguous` result (candidate set) so the caller holds for review. Exclude soft-deleted assets from matching and surface a soft-deleted-only match to the caller. Satisfies: asset-identity-resolution (efficient lookup order, soft-deleted excluded), processing-pipeline (efficient existing-asset lookup). Owns: `core/identity/resolve.go`, `core/identity/resolve_test.go`. Verify: `go test ./core/identity/ -count=1` â€” serial match short-circuits (brand+model/name+model not executed); brand+model fallback when no serial; name+model fallback; ambiguous (>1) returns the candidate set; a soft-deleted asset is not matched; a deleted-only match is surfaced. (Index-backed flat-latency is proven in task 9.1.)
- [x] 3.2 [backend] Field-merge update in `core/identity/resolve.go mergeInto`/`newAsset`: stop stamping `DocType` (removed); stamp `Name`, `AssetCategory`, `CategoryConfidence` (non-nil-only, like the other fields); make `asset_category` sticky â€” do not overwrite a survivor with `CategoryUserSet=true`; an inferred category may replace a lower-confidence inferred one. Satisfies: asset-identity-resolution (field merge on update, sticky category), asset-taxonomy (category correction). Owns: `core/identity/resolve.go` (merge), `core/identity/resolve_test.go`. Verify: `go test ./core/identity/ -run 'Merge|Sticky|NewAsset' -count=1` â€” null extracted fields never erase existing; newer non-empty values win; a user-set category survives a later lower-confidence inference; a new asset is created with name/category/confidence set and no doc_type.

## 4. Processing pipeline (standalone module)

- [x] 4.1 [backend] Content-hash dedupe in `core/processing/dedupe.go` (+ identity-level in `core/identity/dedupe.go`): source-level â€” before extraction, query `sources` for the owner with `sha256 = H` (bounded); if found, load the linked `Document` (by `source_id`) and its `Asset`, and return `Outcome{Kind: OutcomeDuplicate, Duplicate:{SourceID,DocumentID,AssetID,AssetDeleted}}` with **no** new source stored and **no** LLM call; if the asset is soft-deleted set `AssetDeleted=true`. Identity-level â€” new file `core/identity/dedupe.go`: `DedupeHash(sourceHash string, assetID string) (*entity.Document, bool)` â€” when an extraction resolves to an existing asset whose linked document already has the same source hash, return the existing document as a duplicate without modifying any asset field or creating a second document. Satisfies: asset-lifecycle (dedupe on re-upload), asset-identity-resolution (re-upload dedupe), document-ingestion (byte-duplicate upload). Owns: `core/processing/dedupe.go`, `core/processing/dedupe_test.go`, `core/identity/dedupe.go`, `core/identity/dedupe_test.go`. Verify: `go test ./core/processing/ -run Dedupe -count=1` â€” re-upload of a known hash â†’ `OutcomeDuplicate` referencing the existing doc/asset, no new source, no LLM call; re-upload after the asset was soft-deleted â†’ `Duplicate.AssetDeleted=true`; `go test ./core/identity/ -run 'Reupload|Dedupe' -count=1` â€” same-hash doc already linked â†’ no field change, no second document.
- [x] 4.2 [backend] Standalone processing module entry point `core/processing/service.go`: `Process(ctx, Input) (Outcome, error)` composing â€” size check (`ErrTooLarge`) â†’ dedupe (4.1) â†’ `storage.Put` + `Sources.Create` (pre-tx) â†’ parallel extraction + consensus (2.4) â†’ statement classification check (`classification == statement` â†’ `OutcomeStatement` carrying the retained source) â†’ warranty (2.1) + category (2.6) â†’ indexed identity lookup + commit/hold (3.1/3.2): consensus conf â‰¥ `PROCRASTINATOR_INGEST_REVIEW_THRESHOLD` (default 0.7) & definitive match â†’ `InTx{identity.Resolve + Documents.Create}` â†’ `OutcomeCommitted`; ambiguous or below threshold â†’ `reviewer.Hold` (passing the candidate set + per-worker results in the `Provenance` field) â†’ `OutcomeHeldForReview`; all workers failed â†’ `OutcomeFailed` (source retained); enforce the end-to-end budget (`PROCRASTINATOR_PROCESS_TIMEOUT`, default 30s) â€” on overrun hold with partial provenance (per-worker results obtained so far stored in `Provenance`). Define `Input`, `Outcome`, `OutcomeKind`, `Duplicate`, `Provenance` per D1. Delete `core/ingest` (migrate its tests). Extend `repo.Reviewer.Hold` / `HoldInput` with a `Provenance map[string]any` field and persist it to `ingest_reviews.provenance` in `core/review/service.go Hold`. The `api/handlers.go UploadDocument` re-point (â†’ `processing.Process`, map committedâ†’201 / duplicateâ†’200 / heldâ†’202) is done in task 6.3. Satisfies: processing-pipeline (standalone module, outcomes, latency, provenance), document-ingestion (synchronous upload, 201/200), asset-lifecycle (dedupe), asset-taxonomy, llm-extraction. Owns: `core/processing/service.go`, `core/processing/outcome.go`, `core/processing/service_test.go`; `core/ingest/*` (deleted); `commons/repo/review.go` (HoldInput + Provenance), `core/review/service.go` (Hold persists provenance). Verify: `go test ./core/processing/ -run Process -count=1` with fakes â€” one test per outcome (committed, held, duplicate, failed, statement); all-workers-failed retains the source and creates no asset/document; oversize â†’ `ErrTooLarge`; held outcome stores provenance (candidate set + per-worker results) in the review; `go build ./...` succeeds with `core/ingest` removed.

## 5. Asset lifecycle

- [x] 5.1 [backend] Soft-delete + restore + purge in a new `core/lifecycle/service.go`: `SoftDelete(ctx, assetID)` sets `deleted_at=now()` (owner-scoped, 404 on unknown/other-owner, no row removal); `Restore(ctx, assetID)` clears `deleted_at` only within the retention window (passed as constructor param from 6.6's config), 409 beyond; `PurgeExpired(ctx)` hard-deletes assets with `deleted_at < now()-retention` and sets `documents.asset_id=NULL` for their documents (documents + sources retained). Extend `commons/repo` with the needed asset update/lookup options and a documents-link-null helper if not present. Satisfies: asset-lifecycle (soft-delete, restore), asset-registry (soft-deleted excluded from list/get). Owns: `core/lifecycle/service.go`, `core/lifecycle/service_test.go`; `commons/repo/options.go` (if a new option is required). Verify: `go test ./core/lifecycle/ -run 'Delete|Restore|Purge' -count=1` â€” delete hides the asset (list/get 404) but row retained; restore within window recovers fields + document links; restore beyond window â†’ conflict; purge removes only expired soft-deleted rows, nulls their document links, and retains document + source rows.
- [x] 5.2 [backend] Merge/dedupe in `core/lifecycle/merge.go`: `Merge(ctx, survivorID, duplicateID)` in one transaction â€” field strategy (survivor identity + non-null-wins on conflict; null-on-survivor/non-null-on-duplicate copied; `metadata` shallow-merged, survivor wins on conflict; survivor `category_user_set` preserved); document preservation (`UPDATE documents SET asset_id=survivor WHERE asset_id=duplicate` â€” every doc on either asset appears on the survivor, none erased); outcome+audit (duplicate soft-deleted with `merged_into=survivor`, `merged_at=now()`; duplicate excluded from list/search/get and unmatchable; survivor exposes the merged-in assets + when). Satisfies: asset-lifecycle (merge field strategy, document preservation, outcome and audit). Owns: `core/lifecycle/merge.go`, `core/lifecycle/merge_test.go`. Verify: `go test ./core/lifecycle/ -run Merge -count=1` â€” duplicate's serial fills a null survivor serial; survivor's model wins on conflict; survivor's user-set category is not overwritten; a survivor with D1 + duplicate with D2 â†’ survivor's doc list has both; duplicate gets `merged_into`+`merged_at` and is excluded from list/get; survivor detail lists the merged-in asset + timestamp.

## 6. API contract & codegen

- [x] 6.1 [backend] Extend the OpenAPI contract `procrastinator-backend/api/openapi.yaml`: add operations `DELETE /api/users/{userId}/assets/{assetId}`, `POST /api/users/{userId}/assets/{assetId}/restore`, `POST /api/users/{userId}/assets/{assetId}/merge` (body `{duplicate_asset_id}`), `PATCH /api/users/{userId}/assets/{assetId}` (body incl. `asset_category`), `POST /api/users/{userId}/add` (multipart files / text body, optional `account_id` for statement items), and extend `GET /api/users/{userId}/search` (+`/search/quick`) with filter query params (`category`, `brand`, `purchase_from`, `purchase_to`, `warranty_status`, `has_documents`, `doc_classification`); add `include_deleted` (boolean, default `false`) query param to `GET /api/users/{userId}/assets` and `GET /api/users/{userId}/assets/{assetId}`. Update schemas: `asset` gains `name`, `asset_category`, `category_confidence` (+ optional `deleted_at`, `merged_into`, `merged_at`, `merged_assets[]`), **drops `doc_type`**; extend document `classification` enum; add `add_item_outcome` (`asset_committed|held_for_review|duplicate|statement_preview|failed`), `merge_request`, `patch_asset_request`. Keep the `error` envelope on every non-2xx. Satisfies: api-contract (all four requirements), asset-lifecycle, search-experience (typed filters), unified-add. Owns: `api/openapi.yaml`. Verify: `make codegen-go` succeeds (valid OpenAPI, generates); the `asset` schema has no `doc_type` and has the new fields; `GET /assets` and `GET /assets/{assetId}` have the `include_deleted` param.
- [x] 6.2 [backend] Regenerate Go server types from the updated contract: run `make codegen-go` to regenerate `procrastinator-backend/api/gen/openapi.gen.go` (new `ServerInterface` methods + DTOs + chi routes). Satisfies: api-contract (generated types + client SDK). Owns: `api/gen/openapi.gen.go`. Verify: `cd procrastinator-backend && go test ./api/gen/ -run 'TestCodegenDrift|TestNewOperationYieldsScaffolding' -count=1` â€” drift test passes (committed file matches regenerated output).
- [x] 6.3 [backend] Asset lifecycle handlers + read-path soft-delete exclusion `procrastinator-backend/api/lifecycle.go` + `api/handlers.go`: `DeleteAsset` (soft-delete â†’ 204; 404 unknown/other-owner), `RestoreAsset` (200; 409 beyond window; 404), `MergeAssets` (200 with survivor; 404/409), `PatchAsset` (200; user category correction sets `category_user_set=true`). Modify `api/handlers.go`: (a) `GetAsset` returns 404 for soft-deleted assets unless `include_deleted=true` query param is set; (b) `ListAssets` filters `deleted_at IS NULL` by default, includes soft-deleted when `include_deleted=true`; (c) `UploadDocument` re-pointed at `processing.Process` (map committedâ†’201, duplicateâ†’200 with `duplicate:true`, heldâ†’202). Satisfies: asset-lifecycle, asset-registry (soft-deleted excluded from list/get/search), asset-taxonomy (category correction), processing-pipeline (re-pointed handler). Owns: `api/lifecycle.go`, `api/lifecycle_test.go`, `api/handlers.go`, `api/handlers_test.go`. Verify: `go test ./api/ -run 'Delete|Restore|Merge|Patch|UploadDocument|ListAssets|GetAsset' -count=1` â€” delete hides (subsequent get 404 unless `include_deleted=true`); list omits deleted by default, includes with param; restore within/beyond window; merge returns the survivor with both docs; patch sets the category; upload 201/200/202; another-owner id â†’ 404 unchanged.
- [x] 6.4 [backend] Unified add handler `procrastinator-backend/api/add.go`: `Add` binds multipart file(s) and/or a text body + optional `account_id` field; per item, auto-detects (CSV/obvious statement â†’ `statement.Service.Upload(ctx, accountID, filename, data)` â†’ `statement_preview`; else â†’ `processing.Process`), maps `Outcome` â†’ uniform per-item outcome (`asset_committed`/`held_for_review`/`duplicate`/`statement_preview`/`failed`) and returns the array; pasted text is stored as a `text/plain` source before processing; statement items require `account_id` (from the request â€” the add surface includes an account selector). Satisfies: unified-add (single endpoint, auto-detect, uniform feedback), api-contract (unified add). Owns: `api/add.go`, `api/add_test.go`. Verify: `go test ./api/ -run Add -count=1` â€” a receipt image â†’ `asset_committed` (or `held_for_review`); a statement CSV with `account_id` â†’ `statement_preview` (import batch ref); a statement CSV without `account_id` â†’ 400; a byte-duplicate â†’ `duplicate`; a bad payload â†’ `failed`; a mixed batch (2 receipts + 1 statement with account_id) â†’ one outcome per item.
- [x] 6.5 [backend] Enhanced search `procrastinator-backend/api/search.go` + `core/search/service.go` + `commons/repo/search.go`: add `search.Filters{Category, Brand, PurchaseFrom, PurchaseTo, WarrantyStatus, HasDocuments, DocClassification}`; `SearchBackend.SearchAssets` accepts the filters (asset ILIKE fields gain `name`; `warranty_status` computed from `warranty_end` vs `now()`; `has_documents` via a document join); `SearchDocuments` honors `doc_classification`; filters AND with `q` and each other; preserve the type-priority concatenation + quick-is-a-prefix invariant. `api.Search`/`api.QuickSearch` bind the new params. Satisfies: search-experience (filtered and faceted search, consistent search across kinds). Owns: `api/search.go`, `api/search_test.go`, `core/search/service.go`, `core/search/service_test.go`, `commons/repo/search.go` (interface), `infra/postgres/search.go` (or the search query file) (+ test). Verify: `go test ./core/search/ ./api/ -run 'Filter|Warranty' -count=1` â€” `q=microwave&category=appliance` filters; `warranty_status=expiring_within:90` returns only assets whose `warranty_end` is in-window; blank `q` still works; combined filters AND; kind labels present.
- [x] 6.6 [backend] Composition root + config + server wiring `procrastinator-backend/api/cmd/procrastinator/main.go` + `api/server.go`: replace `ingest.New(...)` with `processing.New(factory, workers, storage, maxBytes, threshold, reviewer, retentionDays, candidateLimit, processTimeout)`; construct `lifecycle.New(factory, retentionDays)` and pass both to `api.New(...)`; wire `*lifecycle.Service`, `*processing.Service`, `*statement.Service` into `api.Server` struct + `New`. Add **all** new config vars to `config/config.go` (canonical owner of this file): `PROCRASTINATOR_LLM_WORKERS`, `PROCRASTINATOR_ASSET_DELETE_RETENTION_DAYS`, `PROCRASTINATOR_LOOKUP_CANDIDATE_LIMIT`, `PROCRASTINATOR_PROCESS_TIMEOUT`, `PROCRASTINATOR_INGEST_REVIEW_THRESHOLD` (already exists), `PROCRASTINATOR_BRAND_LEXICON` (optional JSON path) â€” all in the fail-fast pattern. Update the e2e wiring in `e2e/tenancy_e2e_test.go` to the new constructors. Satisfies: processing-pipeline (standalone module wiring), api-contract, asset-lifecycle (config). Owns: `api/cmd/procrastinator/main.go`, `api/server.go`, `config/config.go` (+ test), `e2e/tenancy_e2e_test.go` (constructor wiring only). Verify: `cd procrastinator-backend && go build ./... && go test ./e2e/ -run TestTenancy -count=1` (or the existing boot/wiring test) â€” the server constructs and boots with the processing + lifecycle services; `go test ./config/ -count=1` parses all new vars (default 2 workers, 30-day retention, 10 candidate limit, 30s timeout); `make codegen-drift-check` passes.

## 7. Web platform

- [x] 7.1 [ui] Dependency audit + latest-stable pin + TanStack Table: produce `ui/DEPS-AUDIT.md` listing each UI dependency, its pinned version, the latest stable upstream version, and a justification for any gap (no pre-release/canary); confirm React 19 / Vite 6 / Tailwind 4 / TanStack Query 5 / react-router 7 are on current stable majors and pin them in `package-lock.json`; add `@tanstack/react-table` at its latest stable. Satisfies: web-platform (current latest-stable stack, new libraries at latest stable). Owns: `ui/package.json`, `ui/package-lock.json`, `ui/DEPS-AUDIT.md`. Verify: `cd ui && npm install && npm run build` succeed; `ui/DEPS-AUDIT.md` exists and lists every dep with pinned + latest-stable + justification; `@tanstack/react-table` present in `package.json` at a stable version.
- [x] 7.2 [ui] Regenerate the UI API surface from the updated contract + wrappers: run `make codegen-ts` and `make codegen-ts-client` to regenerate `ui/src/lib/api/generated/paths.d.ts` and `ui/src/lib/api/generated/orval/procrastinator.ts`; re-export the new types in `ui/src/lib/api/schema.ts`; add thin typed wrappers in `ui/src/lib/api/client.ts` for delete/restore/merge/patch/add/search-with-filters. Satisfies: api-contract (client SDK), search-experience (typed filters). Owns: `ui/src/lib/api/generated/paths.d.ts`, `ui/src/lib/api/generated/orval/procrastinator.ts`, `ui/src/lib/api/schema.ts`, `ui/src/lib/api/client.ts` (+ tests). Verify: `cd ui && npx vitest run src/lib/api/codegen.drift.test.ts src/lib/api/codegen.client.drift.test.ts` pass (generated files are codegen output); `npm run test` â€” the new client wrappers unwrap data and map errors.
- [x] 7.3 [ui] Shared feature-rich `DataTable` on TanStack Table `ui/src/components/data-table.tsx`: column **visibility selection**, **multi-column sorting**, and **pagination**, generic over row type + column config; axe-clean; a smoke/integration test. Satisfies: web-platform (shared feature-rich table library), search-experience (feature-rich results table). Owns: `ui/src/components/data-table.tsx`, `ui/src/components/data-table.test.tsx`. Verify: `cd ui && npx vitest run src/components/data-table.test.tsx` â€” hides/reorders columns, multi-column sort, and pagination all work; `vitest-axe` reports no violations.
- [ ] 7.4 [ui] Consolidated primitives: make `ui/src/components/ui/select.tsx` the single select pattern, add one shared upload/dropzone wrapper (`ui/src/components/upload.tsx`, react-dropzone), and keep `ui/src/components/ui/dialog.tsx` + `ui/src/components/confirm-dialog.tsx` as the single dialog pattern; remove any hand-rolled `<Table>` and parallel select/upload implementations. Satisfies: web-platform (consolidated UI primitives). Owns: `ui/src/components/upload.tsx`, `ui/src/components/upload.test.tsx`, and the affected `ui/src/components/ui/**` + page files that had hand-rolled tables (edits only where a duplicate primitive is removed). Verify: `cd ui && npm run test && npm run build` succeed; a grep for hand-rolled `<Table`/native-styled-select outside `data-table.tsx`/`components/ui` returns nothing (no per-page hand-rolled table remains).

## 8. UI features

- [x] 8.1 [ui] Data-layer hooks for the new operations `ui/src/lib/api/hooks.ts`: `useDeleteAsset`, `useRestoreAsset`, `useMergeAsset`, `usePatchAsset`, `useAdd`, `useSearch(filters)` following the existing convention (every query key carries the active user id; requests disabled while no user is active; each mutation invalidates exactly the named prefixes â€” add/delete/restore/merge/patch â†’ `['assets',uid]` + `['asset',uid,id]`, commit/approve also â†’ related lists). Satisfies: api-contract (client SDK), search-experience, asset-lifecycle. Owns: `ui/src/lib/api/hooks.ts`, `ui/src/lib/api/hooks.test.tsx`. Verify: `cd ui && npx vitest run src/lib/api/hooks.test.tsx` â€” query keys include `userId`; each mutation invalidates exactly the specified prefixes; `useSearch` re-queries on filter change.
- [ ] 8.2 [ui] Unified Add surface `ui/src/pages/add/add-page.tsx` at `/add`: one dropzone (file) + one paste-text box + an **account selector** (for statement items â€” required by `statement.Service.Upload`), single prominent surface; calls `useAdd` (passes `account_id` for statement items); renders each item's uniform outcome (asset / held-for-review / duplicate with a restore offer when `asset_deleted` / statement-preview / failed) in one summary with per-item navigation; remove the `/upload` route and the separate statement-import nav entry so the app shell has a **single Add entry**. Update `ui/src/router.tsx` and `ui/src/components/layout/app-shell.tsx` (Add always visible). Satisfies: unified-add (single entry point, uniform result feedback), web-platform (simple-but-modern, no duplicate-flow nav). Owns: `ui/src/pages/add/add-page.tsx`, `ui/src/pages/add/add-page.test.tsx`, `ui/src/router.tsx`, `ui/src/components/layout/app-shell.tsx` (+ test edits). Verify: `cd ui && npx vitest run src/pages/add/ src/components/layout/app-shell.test.tsx` â€” a photo â†’ asset outcome; pasted text â†’ asset outcome; a statement file + selected account â†’ `statement_preview` (no separate page visit); a statement file without account â†’ validation error; a duplicate outcome shows the restore offer; the shell shows one Add entry and the search bar always visible; axe-clean.
- [ ] 8.3 [ui] Search as primary navigation `ui/src/components/search/**` + `ui/src/pages/search/search-results-page.tsx`: a persistent, keyboard-first search bar in the app shell (global shortcut e.g. `/` / âŒ˜-K; full keyboard navigation of results); filter controls (category, brand, purchase-date range, warranty status, has-documents, doc classification) that are **reflected in the URL** (shareable/reloadable); results rendered in the shared `DataTable`, **grouped/labeled by kind** (asset/document/movement/â€¦), each hit navigating to its detail; wire `useSearch(filters)`. Satisfies: search-experience (search as primary nav, filtered/faceted, results table, consistent across kinds), web-platform (primary actions one gesture away). Owns: `ui/src/components/search/search-bar.tsx`, `ui/src/components/search/search-filters.tsx`, `ui/src/components/search/*.test.tsx`, `ui/src/pages/search/search-results-page.tsx` (+ test). Verify: `cd ui && npx vitest run src/components/search/ src/pages/search/` â€” the shortcut focuses the bar and typing filters; typing "cooler master" surfaces the cabinet; a filter combo is encoded in the URL and reloading reproduces the same set; results are kind-labeled and navigable; `warranty_status=expiring within 90 days` shows only in-window assets; axe-clean.
- [ ] 8.4 [ui] Asset list rework `ui/src/pages/assets/asset-list-page.tsx`: render in the shared `DataTable` with **name + brand + model** as distinct columns (+ category, warranty end/status), column visibility/multi-column sort/pagination available; warranty status marked (string date comparison); empty state points to `/add`; row navigates to detail. Satisfies: search-experience (feature-rich results table), asset-taxonomy (asset detail shows what the item is), web-platform (one table component). Owns: `ui/src/pages/assets/asset-list-page.tsx`, `ui/src/pages/assets/asset-list-page.test.tsx`. Verify: `cd ui && npx vitest run src/pages/assets/asset-list-page.test.tsx` â€” name/brand/model render as distinct columns; hiding a column + sorting by purchase date works; the same column controls are present (shared `DataTable`); empty state links to `/add`; axe-clean.
- [ ] 8.5 [ui] Asset detail rework `ui/src/pages/assets/asset-detail-page.tsx`: show the item's canonical **name** alongside brand and model as separate fields, `asset_category` + confidence, the documents list (classification, source filename, upload timestamp), and **delete** (behind confirm), **restore** (when deleted), **merge** (pick the duplicate asset â†’ confirm), and **user category correction** (a select â†’ `usePatchAsset`); 404 not-found state. Satisfies: asset-lifecycle (delete/merge in the UI), asset-taxonomy (category inference and correction, name vs brand/model). Owns: `ui/src/pages/assets/asset-detail-page.tsx`, `ui/src/pages/assets/asset-detail-page.test.tsx`. Verify: `cd ui && npx vitest run src/pages/assets/asset-detail-page.test.tsx` â€” name/brand/model distinct; category shown + confidence; documents list shows classification + source filename; delete is behind confirm and invalidates the list; merge picks a duplicate + confirm; category correction PATCHes and sticks; unknown id â†’ not-found; axe-clean.
- [ ] 8.6 [ui] Adopt the shared `DataTable` on the review queue + statement preview: rework `ui/src/pages/reviews/review-queue-page.tsx` and `ui/src/pages/finance/import-lines-table.tsx` to render through `ui/src/components/data-table.tsx` with the same column-visibility/sort/pagination controls; remove their hand-rolled tables. Satisfies: search-experience (same table across surfaces), web-platform (duplicated tables removed). Owns: `ui/src/pages/reviews/review-queue-page.tsx` (+ test), `ui/src/pages/finance/import-lines-table.tsx` (+ test edits in `import-page.test.tsx`/`import-history-page.test.tsx` if needed). Verify: `cd ui && npx vitest run src/pages/reviews/ src/pages/finance/import-page.test.tsx` â€” the review queue and statement preview render via the shared `DataTable` with column visibility/sort/pagination; the same column controls are available as on the assets list; axe-clean.

## 9. End-to-end verification

- [ ] 9.1 [backend] DB integration tests for the reworked data path `procrastinator-backend/infra/postgres/*_test.go`: **indexed lookup flat latency** (owner with 100 assets vs 10,000 assets â€” the identity-lookup phase for the same document completes within the same order of magnitude, < 5Ã—, proving no full-registry scan); **merge document preservation** at the SQL level (docs on both assets land on the survivor); **soft-delete exclusion** (list/get/search omit deleted; `include_deleted` includes them); **purge** (expired soft-deleted rows removed, their document links nulled, documents + sources retained); **cross-owner isolation** for the new lifecycle/lookup paths (reuse the `rls_test.go`/`search_test.go` topology). Satisfies: processing-pipeline (efficient lookup), asset-identity-resolution (flat lookup), asset-lifecycle (delete/purge), asset-registry (soft-delete). Owns: `infra/postgres/asset_rework_test.go` (new) + any fixture helpers it needs. Verify: `cd procrastinator-backend && go test ./infra/postgres/ -run 'Rework' -count=1` â€” the 10,000-asset lookup is < 5Ã— the 100-asset lookup; merge preserves docs; soft-deleted rows are excluded/re-included; purge retains documents/sources; another owner's rows are never returned.
- [ ] 9.2 [backend] End-to-end ingestion test using `sample-data/` fixtures `procrastinator-backend/e2e/asset_rework_e2e_test.go`: process the AMC contract (`Annual Maintenance Contract.pdf`, "MICRO WAVE OVEN CONVECTION 30BRC2") â†’ asset with canonical name `Microwave Oven`, model `30BRC2`, category `appliance`, AMC document linked (classification `amc`); process a "CABINET COOLER MASTER CD600 BLACK" label (one of the `sample-data/*.jpg` or `sample-data/INVNAG2302754.pdf`) â†’ brand `Cooler Master`, name `Cabinet`, model `CD600`; **dedupe on re-upload** (re-upload the same PDF â†’ `duplicate`, no second asset); **warranty** ("warranty 2 years" + purchase date â†’ `warranty_end = purchase + 2y`); **merge** two duplicate assets â†’ survivor keeps both docs. (Where the LLM is unavailable in CI, drive the same assertions through the `processing` module with a stubbed extractor returning the canonical sample extractions, plus one LLM-gated test tagged for live runs.) Satisfies: processing-pipeline (brand/model, splitting, warranty), asset-taxonomy (category, name), asset-lifecycle (dedupe, merge), document-ingestion (guaranteed linkage). Owns: `e2e/asset_rework_e2e_test.go`. Verify: `cd procrastinator-backend && go test ./e2e/ -run 'AssetRework' -count=1` â€” microwave asset has name/model/category + AMC doc; cooler-master label splits brand/name/model; re-upload returns duplicate (no new asset); warranty_end computed; merge preserves both docs.
- [ ] 9.3 [ui] **End-to-end UI verification (browser-driven, workflow gate):** with the backend running on `:8080` and `cd ui && npm run dev` (proxy `/api` â†’ `:8080`), walk every reworked flow at **desktop and 360px** widths (no page-level horizontal scroll): (a) **/add** â€” drop a `sample-data` photo â†’ asset outcome; paste receipt text â†’ asset outcome; drop a statement CSV â†’ `statement_preview` (then previewâ†’commit in the ledger flow); re-upload the same file â†’ duplicate with restore offer; (b) **/search** â€” global shortcut focuses the bar; search "cooler master" finds the cabinet; apply category + warranty-status filters and confirm they encode in the URL and reproduce on reload; results are kind-labeled and navigate to detail; (c) **asset list** â€” name/brand/model distinct columns; hide/reorder a column; sort by purchase date; paginate; (d) **asset detail** â€” name/brand/model shown separately; category + confidence; documents list; **delete** (confirm) â†’ asset hidden; **restore**; **merge** two duplicates â†’ survivor shows both docs; correct the category â†’ it sticks; (e) switch the active user â†’ data replaces. Capture a screenshot per screen. Satisfies: unified-add, search-experience, asset-lifecycle, asset-taxonomy, web-platform (all UI behaviors). Owns: no source files (verification only; fix any surfaced defect in the owning task's files). Verify: browser-driven pass (ui-impl-worker/ui-reviewer tooling) with screenshots for /add, /search, asset list, asset detail, and the delete/restore/merge flows at both widths; `cd ui && npm run test && npm run build` green. This task is the final gate â€” the workflow is complete only when it passes.

---

# Cycle-2 delta tasks (subspec-3, re-fixed per subspec-2 review)

User-directed cycle-2 delta (jsonb entity storage, fresh migrations, landing,
framework pass, modularization, documents section, reprocess/delete/edit,
duplicate prompt, UX test plan). Tasks reference design decisions D14–D22.

**Dispatch notes.** Ownership for `e2e/tenancy_e2e_test.go` and
`config/config.go` shifts to the delta owners below (10.1/6.6 conventions);
`api/openapi.yaml` owner moves to 10.9 for the cycle-2 contract delta;
9.3 and 11.9 remain the final gates.
- 10.1 **is paired with 10.2–10.8**: the fresh jsonb migrations break all
  code written against the old real-column layout, so 10.1 cannot be
  dispatched alone. Dispatch order: 10.1's migration files are written
  first, but 10.1's full verify only greens once 10.8 (the mechanical
  compile-fix sweep) is done. 10.1 + 10.2–10.8 form one logical unit.
- 10.9 depends on 10.8; 10.10 depends on 10.7–10.8; 10.11 depends on 10.9–10.10;
  10.12 depends on 10.11; 10.13/10.14 depend on 10.12.
- 11.3 **depends on cycle-1 tasks 8.2–8.6 being complete** (it moves the
  `ui/src/pages/**` files those tasks create into `ui/src/features/**`).
  11.2 must precede 11.4–11.7 (hooks/types); 11.6–11.7 must follow 11.3
  (they create files directly in `ui/src/features/**`).
- `core/lifecycle/service.go`, `core/lifecycle/merge.go` and
  `core/identity/resolve.go`: owned by done cycle-1 tasks 5.1/5.2/3.2 —
  **re-owned here** by 10.10 (sequential edit, sticky-field membership
  swap; see D20). No pending cycle-1 task owns them, so no conflict.

## 10. Cycle-2 backend delta

- [ ] 10.1 [backend] **[FOUNDATION — paired with 10.2–10.8, see dispatch notes]** Fresh consolidated baseline migrations in the D14 jsonb shape: DELETE `procrastinator-backend/migrations/00001_identity.sql` … `00006_asset_rework.sql` and write `00001_identity.sql` (users/households/household_members, key+audit real columns + `data jsonb` for payload fields per D14 — `household_members` has no `data` column per the D14 exception; seed users `test-user`, `test-user-b`), `00002_entity_tables.sql` (all 8 entity tables per the D14 column table: key+audit real columns + `data jsonb NOT NULL DEFAULT '{}'`; FK/tenancy indexes; expression indexes — 3-stage identity lookups over `data->>'norm_serial|norm_brand|norm_model|norm_name'`, `sources (owner_id, (data->>'sha256'))`; functional unique indexes replacing real-column uniques; `touch_updated_at()` trigger function + binds on every table with `updated_at`), `00003_row_level_security.sql` (RLS enable + policies re-applied on real owner columns + membership trigger). Down drops everything in reverse. In `e2e/tenancy_e2e_test.go`, change the schema-version gate constant **from `6` to `3`** (the top migration version after the fresh baseline — the gate is the single version check in that file's migration-count assertion). Satisfies: D14/D15 (jsonb + from-scratch baseline). Owns: `migrations/*` (delete + rewrite), `e2e/tenancy_e2e_test.go` (schema-gate constant only). Verify (standalone portion): `cd procrastinator-backend && go test ./infra/postgres/ -run TestMigrate -count=1` applies the baseline up/down cleanly + psql assertions: every entity table has `data jsonb`, no `norm_serial`/`doc_type`/`sha256` real columns remain, the functional unique index exists on assets serial, the `updated_at` trigger fires on UPDATE, RLS denies cross-owner on all 8 tables. (The full `go test ./...` only greens after 10.8; that is expected and documented — 10.1 is not independently dispatchable.)

- [ ] 10.2 [backend] Asset + document jsonb entity swap: per D14, rework the **asset + document** entity layer — `commons/entity/asset.go`, `commons/entity/document.go` keep one canonical struct per entity; payload fields (all non-audit fields per the D14 table) serialize to/from the `data` column via a marshal/unmarshal pairing (audit fields `created_at/updated_at/deleted_at` are excluded from the payload — audit-only, never meaningfully in `data`). Add the `user_set_fields []string` set to the asset payload (replaces `category_user_set` per D20 — a user-set category is `"asset_category" ∈ user_set_fields`). Introduce the per-entity row-mapper file `infra/postgres/mapper_asset.go` (asset + document rows: `[id + key/audit + data json.RawMessage]`, `json.Marshal`/`json.Unmarshal` of the payload); rewrite the asset/document SQL/scan/marshal to key+audit columns. Satisfies: D14 (Go jsonb mapping, asset+document slice). Owns: `commons/entity/asset.go`, `commons/entity/document.go`, `commons/entity/review.go` (payload-fied `Provenance`), `infra/postgres/mapper_asset.go` (+ `_test.go` each). Verify: `go test ./commons/entity/ ./infra/postgres/ -run 'TestMapperAsset|TestMapperDocument|TestPayloadAsset' -count=1` — asset/document round-trips preserve identical jsonb payloads; audit fields excluded from `data`; `user_set_fields` persists.

- [ ] 10.3 [backend] Source + ingest-review jsonb entity swap: per D14 for **sources + ingest_reviews** — payload fields (`filename, content_type, byte_size, storage_path, sha256` / `doc_type, extracted_fields, raw_extraction, confidence, best_matched_asset_id, candidate set, provenance`) into `data`; per-entity row-mapper `infra/postgres/mapper_source.go` (covers both — same file, same owner). Dedupe predicate callers (task 4.1's `core/identity/dedupe.go`) are NOT touched here (mechanical fixes sweep in 10.8). Satisfies: D14 (sources/reviews slice). Owns: `commons/entity/source.go`, `commons/entity/review_payload.go`, `infra/postgres/mapper_source.go` (+ tests). Verify: `go test ./infra/postgres/ -run 'TestMapperSource|TestMapperReview' -count=1` — round-trips identical; sha256 lives only in `data`.

- [ ] 10.4 [backend] Financial + movement jsonb entity swap: per D14 for **financial_accounts + money_movements** — payload fields per the D14 table into `data`; per-entity row-mapper `infra/postgres/mapper_finance.go`. Satisfies: D14 (finance slice). Owns: `commons/entity/finance.go`, `commons/entity/movement.go`, `infra/postgres/mapper_finance.go` (+ tests). Verify: `go test ./infra/postgres/ -run 'TestMapperAccount|TestMapperMovement' -count=1` — round-trips identical; FK columns (`source_account_id`, `destination_account_id`, `linked_document_id`) stay real.

- [ ] 10.5 [backend] Import jsonb entity swap: per D14 for **import_batches + import_lines** — payload fields into `data`; `batch_id`/`account_id`/`source_id` stay real; per-entity row-mapper `infra/postgres/mapper_import.go`. Satisfies: D14 (import slice). Owns: `commons/entity/import.go`, `infra/postgres/mapper_import.go` (+ tests). Verify: `go test ./infra/postgres/ -run 'TestMapperBatch|TestMapperLine' -count=1` — round-trips identical.

- [ ] 10.6 [backend] Identity-table jsonb entity swap: per D14 for **users + households + household_members** — `households.display_name` into `data`; `household_members` has no `data` column (the one D14 exception — no payload change needed); per-entity row-mapper `infra/postgres/mapper_identity.go`. Satisfies: D14 (identity slice). Owns: `commons/entity/user.go`, `commons/entity/household.go`, `infra/postgres/mapper_identity.go` (+ tests). Verify: `go test ./infra/postgres/ -run 'TestMapperUser|TestMapperHousehold' -count=1` — round-trips identical.

- [ ] 10.7 [backend] Repo predicate expression rework: repoint **every** `Filter`/`Where` predicate in `commons/repo/**` (`options.go` predicates, per-repo filter helpers) from real-column comparisons to the jsonb expression form (`(data->>'x') = / ILIKE / <=>` date arithmetic per D14's indexing list); update `commons/repo/search.go` signature types (filter shapes unchanged, backed by expression SQL incl. the D10 structured filters). Rewrite the generic predicate engine in `infra/postgres/repository.go` (`addFilterCond`, `validateFilters`, `validOps`, the `pgRepository[T]` struct) to emit expression predicates, and replace the per-entity `fieldCols`/`orderCols` whitelists with real column names + expression forms in the per-entity filter configs: `infra/postgres/repos.go` (assetFieldCols etc. + `AssetRepository.SearchAssets` ILIKE SQL → name/brand/model over `data`), `infra/postgres/finance.go` (accountFilters/movementFilters/importBatchFilters/importLineFilters + SearchAccounts/SearchMovements/SearchImportBatches SQL), `infra/postgres/finance_queries.go` (`DocumentRepository.SearchDocuments` incl. the warranty/has_documents/doc_classification expression SQL), `infra/postgres/household_repo.go` (householdFilters), `infra/postgres/review_repo.go` (reviewFilters), and the `searchBackend` forwarder in `infra/postgres/factory.go` (signature glue only). Satisfies: D14 (expression filters), D16-adjacent search contract unchanged. Owns: `commons/repo/**`, `infra/postgres/repository.go` (+ test), `infra/postgres/repos.go`, `infra/postgres/finance.go`, `infra/postgres/finance_queries.go`, `infra/postgres/household_repo.go`, `infra/postgres/review_repo.go`, `infra/postgres/factory.go` (searchBackend forwarder only). (No other delta task claims these files: 10.2–10.6 own only their `mapper_*.go` new files + `commons/entity/*`; 10.10 owns the new `infra/postgres/documents_status.go`; 10.8's list `core/*`/`api/*`/`e2e/*` is disjoint.) Verify: `go test ./infra/postgres/ ./commons/repo/ -count=1` — predicate coverage: owner/key options unchanged (real columns); payload-field filters emit `data->>'…'` expressions; search filters (category/brand/warranty-status/has-documents/doc-classification) return identical results against the jsonb fixtures.

- [ ] 10.8 [backend] Mechanical compile-fix sweep [EXECUTES AFTER 10.2–10.7; completes the 10.1 pair]: fix every compile break outside the ownership sets above caused by the entity-layer swap — **specific files, no catch-all**: `procrastinator-backend/core/identity/{resolve.go,dedupe.go}`, `core/processing/{service.go,dedupe.go}`, `core/lifecycle/{service.go,merge.go}`, `core/search/service.go`, `core/review/service.go`, `core/statement/{service.go,parser helpers}`, `api/{handlers.go,add.go,search.go,lifecycle.go,server.go}`, `api/cmd/procrastinator/main.go`, `e2e/*_test.go`. Constraint: **mechanical field-access/marshal changes only — no behavior change** (behavior changes belong to 10.9+). Satisfies: D14 (compile-green pair completion). Owns: the files listed above (edit-only, mechanical). Verify: `cd procrastinator-backend && go build ./... && go vet ./...` clean; `go test ./... -count=1` green (the 10.1+10.2–10.8 pair completes here); behavior is pinned by existing suite.

- [ ] 10.9 [backend] Cycle-2 OpenAPI contract **+ codegen** (D16 + D20/D21 payloads): in `procrastinator-backend/api/openapi.yaml` add `GET /api/users/{userId}/documents` (+`status`, `q`, pagination — statuses `processed|in_review|failed|asset_less` + linked asset ref), `DELETE /api/users/{userId}/documents/{documentId}`, `POST /api/users/{userId}/documents/{documentId}/reprocess` (body `{comment?}`), `POST /api/users/{userId}/sources/{sourceId}/import-statement` (body `{account_id}`), `POST /api/users/{userId}/assets/{assetId}/reprocess` (+`document_ids[]` optional subset), `POST /api/users/{userId}/documents/{documentId}/restore`, extend `PATCH /assets/{assetId}` to the structured core (name/brand/model/serial/purchase_date/warranty_end/price/currency/category/metadata w/ sticky `user_set_fields` semantics), and extend the add outcome union with `statement_pending_account` (carrying `source_id`); restate the asset/document/review schemas to mirror the entity payload (D14 jsonb nesting — metadata/extracted_fields/raw_extraction/provenance stay nested objects, no flattening; `merged_assets[]` stays the documented D16 computed-field exception); every non-2xx keeps the `error` envelope. **Codegen absorbed here (no separate codegen task):** re-run `make codegen-go` (the exact command recorded in task 6.2) exit 0 and commit the regenerated `api/gen/openapi.gen.go` — nothing hand-edited in `api/gen/`. Satisfies: api-contract, document-management, asset-lifecycle, asset-registry (extended edit), unified-add (pending-account outcome). Owns: `api/openapi.yaml`, `api/gen/openapi.gen.go` (regen output only). Verify: `make codegen-go` exit 0; `go test ./api/gen/ -run 'TestCodegenDrift|TestNewOperationYieldsScaffolding' -count=1` PASS (drift + locked regeneration).

- [ ] 10.10 [backend] Documents domain service + sticky-field enforcement: new `core/documents/` — owner-scoped document status join per D20 (source → document → review → asset: `processed | in_review | failed | asset_less` + linked asset display name), status filters + free text; `SoftDelete`/`Restore` (retention-window aware, reusing the assets retention config; restore keeps asset link untouched); `ListStuck` helpers hitting `failed|asset_less`. Extends `core/processing` with `Reprocess(ctx, sourceID, note string) (Outcome, error)` — reuses retained source bytes (no new source row), per-item companion note as extraction context; consensus/gate/merge rules untouched; sticky `user_set_fields` honored on commit (D20/D5 generalization). Sticky generalization surgery (**files re-owned from done 5.1/5.2/3.2, see dispatch notes**): swap the `category_user_set` boolean check for `user_set_fields` membership in `core/lifecycle/service.go`, `core/lifecycle/merge.go`, `core/identity/resolve.go` (merge path semantics unchanged for category = the single-member special case). Implement the status-join query in a new `infra/postgres/documents_status.go` (NOT in `commons/repo/**`, which is owned by 10.7). Satisfies: document-management (status join, reprocess, soft-delete; fix for the stuck-docs defect), D20 (sticky generalization). Owns: `core/documents/service.go` + `_test.go`, `infra/postgres/documents_status.go` + `_test.go`, `core/processing/service.go` + `core/processing/reprocess_test.go` (additive Reprocess), `core/lifecycle/{service.go,merge.go}` + `core/identity/resolve.go` (sticky membership swap + their `_test.go`s). Verify: `go test ./core/documents/ -run 'Status|Stuck|List' -count=1`; `go test ./core/processing/ -run Reprocess -count=1` — (a) reprocess of a failed source runs through consensus + can commit (no new source, comment in provenance), (b) low-confidence re-run → held, (c) commit preserves the existing document↔asset link and updates fields per merge rules (user-set fields not overwritten); `go test ./core/lifecycle/ ./core/identity/ -run 'Merge|Sticky|Delete|Restore|Purge' -count=1` — `user_set_fields ⊇ {asset_category}` behaves exactly as the old `category_user_set=true`; document soft-delete excludes from asset document lists.

- [ ] 10.11 [backend] Documents + extended asset-edit + add handlers (absorbs the old separate statement-pending-account task — one owner, no shared-file conflict): in `api/documents.go` (+ `api/documents_test.go` — pre-modular split; modularization is 10.12): `GET /documents` (pagination + `status`/`q` filters; per-row card data wired to the document-management shape), `DELETE /documents/{documentId}` (soft-delete; 204; 404 unknown/other-owner), `POST /documents/{documentId}/restore`, `POST /documents/{documentId}/reprocess` (comment; 200/202 per usual outcome map), `POST /sources/{sourceId}/import-statement` (account required → import batch preview; source must be statement-classified), `POST /assets/{assetId}/reprocess` (loops documents through `Reprocess`, merges outcomes); extended `PATCH /assets/{assetId}` handler in `api/lifecycle.go` (+ additive `api/lifecycle_test.go` subtests — structured core + sticky `user_set_fields` marking). Also `POST /add` in `api/add.go` (+ `api/add_test.go`) per D20/D21: multi-file companion-text items accepted (`files[]` with a shared `note`) and text-alone; **outcome union: a statement item without `account_id` no longer 400s the whole request — it returns `statement_pending_account` (with the retained `source_id`; the request is 200 so the UI can prompt inline); the statement commits only via `POST /sources/{sourceId}/import-statement`; CSV without an account also yields `statement_pending_account`** (deterministic CSV routing, no LLM). `api/handlers.go` only for the outcome-mapping glue touched by the above. D17 (never ask before input). Satisfies: document-management, asset-lifecycle, landing-experience (deferred account selection), unified-add (companion text + pending account), api-contract. Owns: `api/documents.go` + test, `api/lifecycle.go` + `api/lifecycle_test.go` (additive), `api/add.go` + `api/add_test.go`, `core/processing/service.go` (`OutcomeStatement` carries `AccountRequired bool`; owned sequentially after 10.10), `api/handlers.go` (glue only). Verify: `go test ./api/ -run 'Document|Reprocess|ImportStatement|PatchAsset|AddItems|PendingAccount' -count=1` — documents list shows all four statuses w/ linked asset card data + filtering; delete/restore window; reprocess with/without comment; asset-reprocess merges across documents; extended PATCH accepts name/model/metadata and sticks as user-set; a statement CSV with no account → `statement_pending_account` (source_id present, request 200) then import-statement with an account commits; a non-statement item never surfaces the account request; another-owner 404s unchanged.

- [ ] 10.12 [backend] Modular restructure of `api/` (after 10.9–10.11): split the flat handler files into domain subpackages `api/assets`, `api/add`, `api/search`, `api/documents`, `api/reviews`, `api/finance`, `api/lifecycle`; root `api/` keeps composition/server scaffolding, `api/gen`, `api/cmd`, contract files; handler imports of the openapi `StrictServerInterface` remain satisfied (method receivers composed onto the root); `wire_parity_test.go` + codegen drift tests green. Satisfies: code-organization (api subpackages). Owns: `api/{assets,add,search,documents,reviews,finance,lifecycle}/*.go` (moved handler files + test moves), `api/handlers.go` (deleted/absorbed), `api/server.go` (method composition adjustments only). Verify: `go build ./...` && `go test ./api/... -count=1` && `make codegen-drift-check`; grep gate: `procrastinator-backend/api/*.go` (root level) contains no domain handler logic (only server.go, errors/dto scaffolding).

- [ ] 10.13 [backend] core/ boundary verification record: package responsibility statements for `core/{identity,processing,lifecycle,search,review,statement,documents}` in `core/BOUNDARIES.md`. The behavioral pin is **a Go test in `core/documents/boundary_test.go`** that (a) asserts at compile time that `Reprocess` is a method on `processing.Service` and the document status-join query function is defined in `infra/postgres` or `core/documents` — via a simple compile/call assertion (`var _ func(ctx, sourceID, note string) (processing.Outcome, error) = svc.Reprocess` style) with no behavior simulation — and (b) a `go test` import-graph check that the reprocess/status-join files do not import sibling `core/*` domain packages other than `commons/*` (grepping the source import blocks in-test). Satisfies: code-organization (core verification). Owns: `core/BOUNDARIES.md`, `core/documents/boundary_test.go`. Verify: doc lists per-package responsibilities; `go test ./core/ -run Boundary -count=1` (and the full suite) green.

- [ ] 10.14 [backend] Cycle-2 DB integration tests (`infra/postgres/documents_status_test.go` + `infra/postgres/jsonb_baseline_test.go`): (a) jsonb baseline isolation (per-table expression-filter correctness + cross-owner never-returned), (b) document status joins (each of the 4 statuses observable at the SQL level along the sources→documents→reviews join), (c) reprocess-in-place (no second source row; comment captured in provenance/document payload; merge rules respected at SQL level), (d) document soft-delete/restore/purge paths vs the real `deleted_at` column, (e) retained-source statement import via the new sources endpoint. Satisfies: document-management, asset-lifecycle, D14/D15 verification. Owns: `infra/postgres/documents_status_test.go`, `infra/postgres/jsonb_baseline_test.go` (+ fixture helpers). Verify: `go test ./infra/postgres/ -run 'Status|Jsonb' -count=1` all pass; cross-owner rows never returned; reprocess never inserts a second source row for an existing source.

## 11. Cycle-2 UI delta

- [ ] 11.1 [ui] Framework majors upgrade (D18/D16): bump Vite → 8.x, `@vitejs/plugin-react` → 6.x, TypeScript → 7.x, vitest → 5.x, jsdom → 30.x, `@testing-library/jest-dom` → 7.x (each re-verified `npm view` latest stable, no canary); keep Vite+React 19+react-router 7+Tailwind 4 (the framework-swap result from D18: "keep, upgrade majors"); rewrite `ui/DEPS-AUDIT.md` with framework-swap rationale + final pinned versions. Fix fallout in `ui/src/**` (config, test config, vitest 5 API drift) confined to mechanical/testable edits. Satisfies: web-platform (framework-swap evaluation adopted; latest-stable). Owns: `ui/package.json`, `ui/package-lock.json`, `ui/DEPS-AUDIT.md`, `ui/vite.config.*`, `ui/tsconfig*`, `ui/src/**` (mechanical vitest/jsdom/test-updates only). Verify: `cd ui && npm install && npm run test && npm run build` all green; `ui/DEPS-AUDIT.md` records the swap-vs-upgrade decision + every dep at pinned vs latest-stable with justification.
- [ ] 11.2 [ui] TS codegen regen + wrappers for the cycle-2 contract: re-run `make codegen-ts` / `codegen-ts-client` (the exact `npx` commands from task 7.2) against 10.9's contract; re-export the new types in `ui/src/lib/api/schema.ts`; add typed wrappers + hooks for the new operations (documents list/status/delete/reprocess, asset reprocess, extended PATCH payload, sources-based statement import, `statement_pending_account` outcome shape), building new hooks in `ui/src/lib/api/hooks.ts` with the established invalidation prefixes (`['documents',uid]`, `['asset',uid]`, `['search',uid,...]` reuse) and the existing user-scoping convention. Satisfies: api-contract, document-management, unified-add, search-experience. Owns: `ui/src/lib/api/generated/paths.d.ts`, `ui/src/lib/api/generated/orval/procrastinator.ts`, `ui/src/lib/api/schema.ts`, `ui/src/lib/api/client.ts`, `ui/src/lib/api/hooks.ts` (+ drift/controller tests). Verify: `cd ui && npx vitest run src/lib/api/codegen.drift.test.ts src/lib/api/codegen.client.drift.test.ts src/lib/api/hooks.test.tsx` PASS (generated files codegen output; new hooks invalidate exactly the named prefixes w/ user-scoped keys).

- [ ] 11.3 [ui] Feature-folder reorg **[DEPENDS ON 8.2–8.6 COMPLETE — see dispatch notes]**: move `ui/src/pages/*` (including the files created by the done cycle-1 tasks 8.2–8.6) into `ui/src/features/{add,search,assets,reviews,finance,documents,landing}/` with co-located feature hooks/components inside each folder; `ui/src/components/` keeps shared primitives + the app shell/design system only; route bindings (`ui/src/router.tsx`) point into the features folders; `ui/src/lib/api/hooks.ts` shared-level hooks stay shared. Satisfies: code-organization (UI feature folders). Owns: `ui/src/features/**` (moved files), `ui/src/pages/**` (deleted), `ui/src/router.tsx`, `ui/src/components/**` (feature-specific files moved out only). Verify: `cd ui && npm run test && npm run build` green; grep gate: no page/component/hook code left outside `features/**` that belongs to a feature (a feature-specific hook imported from `components/**` fails the gate); drift tests (codegen) still pass.
- [ ] 11.4 [ui] Landing page + hamburger-only shell (D17): implement `ui/src/features/landing/landing-page.tsx` as the `/` route — large prominent search bar on top that navigates to search results on submit; big centered [+] trigger revealing exactly 📷 Camera / 📎 Upload / ✎ Text (≥44 px touch targets, no doc-type/account pre-selection); labeled Insights placeholder section below; remove the `/` redirect-into-list behavior; `/assets`, `/documents`, `/finance/*`, `/search`, `/ingest/reviews` remain their own routes navigable from the navigation sheet. Rework the shell (relocated by 11.3 into `ui/src/features/shell/`) to: top bar only, hamburger on every screen (opens a sheet with the nav links + active-user control + theme toggle), **no permanent sidebar on any screen**, search bar visible on the landing page (not shell-persistent per the spec: the shell keeps the hamburger sheet; a persistent search prepend acceptable only on non-landing screens per search spec). Camera capture flows land in the same `useAdd` file path as Upload (mobile capture), Text opens the paste box; Upload/Camera include a companion-text field for each selected file(s) batch combined into one `add` item (multiple files + shared companion note + text-alone path already covered by 10.11). Satisfies: landing-experience (all 7 scenarios), unified-add (no pre-selection, companion text). Owns: `ui/src/features/landing/**`, `ui/src/features/add/*` (page-layout additions inside the add feature folder created by 11.3 — this task creates the landing/picker entry files only), `ui/src/features/shell/**`, + test edits in relocated test files. Verify (final locations): `cd ui && npx vitest run src/features/landing/ src/features/shell/` — `/` renders search-bar/[+]/Insights (no list content, no sidebar); [+] reveals exactly the 3 methods (each ≥44px); adding a photo-with-note produces a single outcome item; statement without account surfaces the inline account picker (no pre-selection); returning to `/` restores landing state; no sidebar at either width; axe-clean.
- [ ] 11.5 [ui] Dark mode toggle (D22): theme toggle (light/dark/system) in the navigation sheet + landing top bar; Tailwind v4 `@custom-variant dark` + shadcn class-token strategy; persisted, flash-free first paint; every screen audited in both themes. Satisfies: web-platform (dark mode, quality bar). Owns: `ui/src/features/shell/theme*` (+ test), `ui/src/index.css` (dark token classes only), token/theming-affecting component tweaks confined to `ui/src/components/ui/**`. Verify: `cd ui && npx vitest run src/features/shell/theme` — toggle cycles light/dark/system, persists across reload, no FOUC; screenshots at desktop+360px in both themes axe-clean.
- [ ] 11.6 [ui] Documents feature (D20): `ui/src/features/documents/documents-page.tsx` at `/documents` — lists every ingested source with label, ingest timestamp, **status badge** (`processed`/`in_review`/`failed`/`asset_less`), the linked asset card; status + free-text filters (status: All|Processed|Unprocessed|Review per the spec's vocabulary); mobile (≤360px) renders as cards with row-anchored ≥44px actions via the single shared action-menu primitive; desktop renders the shared `DataTable` with the same columns; per-row actions: **Reprocess** (modal w/ optional comment → `useReprocessDocument`), **Delete** (behind confirm → `useDeleteDocument`), **open linked asset**, **open review**; empty-state CTA to `/add`. Satisfies: document-management (all views/statuses/actions), code-organization (feature folder). Owns: `ui/src/features/documents/**` (+ tests). Verify: `cd ui && npx vitest run src/features/documents/` — processed/in_review/failed/asset_less items all render with correct badges + linked asset; status filter narrows; unprocessed = failed+asset_less; reprocess calls the endpoint w/ comment; delete behind confirm; cards at 360px, shared DataTable at desktop, consistent action menu, ≥44 px touch targets; axe-clean.
- [ ] 11.7 [ui] Duplicate reprocess prompt + asset reprocess/edit affordances (D21 + D20): in the add feature (`ui/src/features/add/**`, page-layout prompt components distinct from 11.4's picker entry), every `duplicate` item renders the prompt (existing file + linked asset name + **Reprocess** w/ optional comment vs **Keep existing**; a third **Restore** option when `asset_deleted=true`); `statement_pending_account` items surface the inline account picker (deferred selection) then call `useImportStatement(sourceId, accountId)`; asset detail (`ui/src/features/assets/**`) gains **Reprocess** (whole asset / per-documents subset) and the extended **Edit** form (name/brand/model/purchase/warranty/price/currency/category/metadata) via `usePatchAsset` with sticky `user_set_fields` semantics shown (edited fields visually marked; not overwritten). Satisfies: unified-add/document-ingestion (duplicate user-decision), asset-lifecycle (reprocess/edit/delete), asset-registry (extended edit). Owns: `ui/src/features/add/duplicate-prompt*.tsx` (+ tests), `ui/src/features/assets/asset-detail-reprocess-edit*` (+ test edits in `asset-detail-page.test.tsx`). Verify: `cd ui && npx vitest run src/features/add/ src/features/assets/` — a duplicate outcome always renders the prompt (never silently badges); Reprocess triggers reprocess w/ comment; Keep existing is a no-op; `asset_deleted=true` shows Restore; a statement outcome w/o account shows the inline picker and commits on selection; asset reprocess runs per the outcome map; edit PATCHes the structured core and edited fields render as user-set; axe-clean.
- [ ] 11.8 [ui] **UX test plan document (the gating artifact — MAJ-6 fix)**: produce `ui/UX-TEST-PLAN.md` — the written UX test plan the web-platform spec requires gating on. Content: a checklist enumerating every behavior from cycle-2 (`/` landing layout, add via Camera/Upload/Text, companion-text variations, dark mode on/off, `/documents` statuses + filters, per-row actions, duplicate reprocess prompt incl. `asset_deleted` Restore, asset reprocess + extended edit sticky semantics, search-as-nav from the landing root with URL-encoded filters, review queue, finance import flow), each item with **its expected result at 360px and at desktop, in light and dark themes**, plus the mobile-first quality bar checks (≥44px touch targets, cards-not-tables at ≤360px, no page-level horizontal scroll). Review the document before implementation sign-off; task 11.9 executes it. Satisfies: web-platform (UX test-plan gate — the plan document exists before the E2E pass runs). Owns: `ui/UX-TEST-PLAN.md` (+ `_snapshot/` if screenshots are referenced). Verify: document exists and covers every behavior listed above with per-width/per-theme expected results; 11.9 passes only against this plan. Implementation SHALL NOT be signed off until the plan exists and passes (see web-platform spec).
- [ ] 11.9 [ui] **[FINAL GATE]** Browser-driven E2E pass **executing the 11.8 plan** (extends 9.3): with the backend running, walk every item in `ui/UX-TEST-PLAN.md` at desktop and 360px: `/` landing (search-first/[+]/Insights; never a list), add via Camera/Upload/Text incl. companion-text variations, dark mode on/off themed captures, `/documents` statuses + filters + per-row reprocess/delete actions, the duplicate reprocess prompt (Reprocess vs Keep existing, Restore offer when deleted), asset reprocess (asset + per-document) + extended edit (sticky semantics), search-as-nav from the landing root with URL-encoded filters, review queue, finance flows — verifying the mobile-first quality bar and the landing-experience scenarios. Any defect found is fixed in the owning task's files and the plan is re-run until it passes with zero failures. Satisfies: web-platform (UX test-plan gate execution), landing-experience, document-management, asset-lifecycle/registry, unified-add. Owns: no source files (verification only). Verify: browser-driven pass executing `ui/UX-TEST-PLAN.md` item-by-item (ui-impl-worker/ui-reviewer tooling) with screenshots captured per screen in both themes at both widths; `cd ui && npm run test && npm run build` green; this pass + 9.3 together close the workflow.
