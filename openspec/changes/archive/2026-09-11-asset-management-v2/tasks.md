## 1. Backend — fresh jsonb schema

- [x] 1.1 Replace `procrastinator-backend/migrations/00001–00006` with a fresh `00001_schema.sql` defining all entity tables in the uniform jsonb shape per design D1 (keys/FK/RLS columns incl. `assets.deleted_at`, audit, single `payload jsonb` per table), FKs (`documents.asset_id` ON DELETE SET NULL, `documents.source_id`, `money_movements.account_id`/`import_batch_id`, `import_batches.account_id`/`source_id`, `ingest_reviews.source_id`), partial unique norm-serial expression index, and `pg_trgm` extension. **Owns:** `procrastinator-backend/migrations/*`. [backend] **Verify:** fresh `migrate_test.go` run against a clean Postgres → all tables exist in uniform shape; inspect one asset row = id/owner_id/owner_household_id/created_at/updated_at/payload.
- [x] 1.2 Add jsonb expression btree indexes (identity-resolution paths norm_serial/brand/model/name incl. owner-composites, authority-scoped sha256 dupe lookup, documents status, import_batches state) and `pg_trgm` GIN expression indexes (assets name/brand/model/serial; accounts name/type/institution; movements description/reference; sources filename) per design D2. **Owns:** `procrastinator-backend/migrations/*`. [backend] **Verify:** EXPLAIN test asserting stage-1 serial lookup and docs-status filter use the expected indexes.
- [x] 1.3 Rewrite `infra/postgres/migrate_test.go` schema assertions to the fresh uniform shape (drop legacy `doc_type`/column asserts; assert payload-not-null default `'{}'`, FK SET-NULL on `documents.asset_id`, unique partial serial index) and keep `rls_test.go`/`e2e/tenancy_e2e_test.go` passing against the new DDL. **Owns:** `procrastinator-backend/infra/postgres/migrate_test.go`, test targets. [backend] **Verify:** `go test ./infra/postgres ./e2e` green.

## 2. Backend — codec/repository layer

- [x] 2.1 Implement the generic payload codec per design D3 (per-kind metadata: key columns, `"data"` root path, field map; marshal/unmarshal entity Data ↔ payload; money as JSON numerics) plus unit tests incl. decimal round-trip `39999.99`/`INR`. **Owns:** `procrastinator-backend/infra/postgres/payload_codec.go`, `payload_codec_test.go`. [backend] **Verify:** unit tests pass; codec round-trips every entity kind's Data without loss.
- [x] 2.2 Rewire `repository.go` selectors/filter/order assembly and update `filterConfig` whitelist maps (columns → validated payload-path expressions) per design D3. **Owns:** `procrastinator-backend/infra/postgres/repository.go`, `repos.go`, `scope.go`, touched tests. [backend] **Verify:** existing `repository_test.go`, `generic_test.go`, `finance*` tests, soft-delete filter tests pass unchanged in behavior.
- [x] 2.3 Update per-kind repo files (`finance.go`, `review_repo.go`, `household_repo.go`, `user_registry.go`, `scans.go`, etc.) to codec-driven maps. **Owns:** `procrastinator-backend/infra/postgres/*` repo files. [backend] **Verify:** `go test ./infra/postgres/...` green; identity-resolution suite (`resolve_test.go`) green.
- [x] 2.4 Add the named `commons/*` changes per design D3 (scoped list): `commons/entity/document.go` gains `UserDirective string` (codec-mapped to `data.user_directive`); `commons/repo` gains the `PayloadCodec` interface consumed by the infra codec; no other `commons/*` struct or SQL-assembly changes. **Owns:** `procrastinator-backend/commons/entity/document.go`, `commons/repo/payload_codec.go`, touched entity tests. [backend] **Verify:** entity unit tests incl. `Document.UserDirective` round-trip through the codec; `go build ./...`.

## 3. Backend — API remodel + modularization

- [x] 3.1 Re-model `api/openapi.yaml` to entity-shaped resource schemas (`{ id, …, data, createdAt, updatedAt }`) for all entities + new document-reprocess/dupe-choice schemas; regenerate `api/gen` and update `dto.go`/`finance_dto.go` remapping + `wire_parity_test.go`/lockstep tests. **Owns:** `procrastinator-backend/api/openapi.yaml`, `api/openapi_test.go`, `api/dto.go`, `api/finance_dto.go`, `api/wire_parity_test.go`, `api/gen/*`. [backend] **Verify:** `go build ./...`; lockstep + wire-parity tests pass; asset fetch returns `data`-nested payload one-to-one with storage.
- [x] 3.2 Split flat `api/` into `api/assets|add|search|documents|finance|reviews|households|tenancy` + shared `api/httpx|gen|docs|dto` with import-path updates only; composition stays in root `server.go`. **Owns:** `procrastinator-backend/api/**` (move). [backend] **Verify:** `go test ./api/...` green post-move with no behavioral diffs; wire parity + codegen lockstep still hold, verified explicitly with `go test ./api -run TestWireParity` and the openapi lockstep test (`go test ./api -run TestOpenAPI*`).
- [x] 3.3 Update ingest handlers per document-ingestion delta: duplicate content-hash upload returns `409` with the named `DuplicateReport` schema (design D4: `code:"duplicate"`, `existing_document_id`, `existing_source_filename`, `existing_source_uploaded_at`, `existing_asset_id?`, `prompt{reprocess_uri, keep_uri, expires_at, timeout_toast}`) instead of silently marking duplicate; remove doc-type/account pre-selection params; accept optional free-text note alongside the file (multipart form field), persist it as `documents.payload.data.user_directive` (entity field `Document.UserDirective` from task 2.4), pass it to `extractor.Extract(ctx, source, directive)` and **create** `documents.payload.data.pending_choice = { state:"pending", outcome:"", created_at, expires_at }` on duplicate detection (creation only — never resolves; mutators live in 3.4, see D7a). **Owns:** `procrastinator-backend/api/add*` (new location), related tests. [backend] **Verify:** happy upload returns 201 entity-shaped asset; note-with-upload: `data.user_directive` persisted **and** stub-extractor test asserts the extraction prompt contains the note text; note-less upload prompts with empty directive; duplicate upload's 409 body matches `DuplicateReport` fields incl. `prompt.expires_at`; `pending_choice` row created with `state:"pending"`; no pre-selection params; legacy silent-mark path removed (test).
- [x] 3.4 Implement documents-section backend per documents-section spec (documents list w/ status filter + filename search, reprocess with optional comment → written to the same `data.user_directive` directive channel, delete w/ detach, asset reprocess), and the **`pending_choice` mutators + clock per D7a**: reprocess/keep endpoints (`POST …/documents/{id}/reprocess|keep`), the 60 s sweeper resolving expired `pending` rows to `keep_existing` + emitting the verbatim `timeout_toast` event, and the lazy guard `pending AND expires_at < now() → keep_existing` on any read/write path.  **Owns:** `procrastinator-backend/api/documents/*`, sweeper + composition-root wiring, touched tests. [backend] **Verify:** handler tests: filter-by-status; reprocess-with-comment persisted to `data.user_directive` in a stub-extractor run; in-flight reprocess attempt returns **409** (reject without duplicating work, machine-checkable status); delete detaches asset preserved; expired pending resolved `keep_existing` by sweeper (timer test with fake clock); restart recovery (fresh process sees already-expired pending and resolves, no lost state); legacy silent-mark path absent.

## 4. UI — toolchain pass

- [x] 4.1 Re-verify current latest stable versions for npm dist-tags against the spec table, record verified versions+date in task note, then upgrade: Vite 8.2 Rolldown + `@vitejs/plugin-react` v6 `reactCompilerPreset`, React 19.2.8, react-router v8 migration, TanStack Query 5.x, Tailwind 4.3 CSS-first `@theme`, lucide-react 1.x, shadcn CLI 4.x refresh, vitest 5 + `@testing-library/dom`, Node ≥22.12. **Owns:** `ui/package.json`, `ui/vite.config.*`, `ui/src/index.css` (`@theme` entry). [ui] **Verify:** app builds with react compiler enabled; vitest suite green; smoke run of the app. *(verified 2026-09-09: vite 8.2.2, react/react-dom 19.2.8, react-router 8.3.1, @tanstack/react-query 5.102.8, tailwindcss+@tailwindcss/vite 4.3.3, lucide-react 1.42.0, shadcn 4.21.0, vitest 5.0.0, @testing-library/dom 10.4.1, @vitejs/plugin-react 6.1.1, babel-plugin-react-compiler 1.0.0 + oxc-transform-react 0.149.0; build green w/ compiler active, vitest 33 files/314 tests green)*
- [x] 4.2 Migrate component styles to Tailwind v4 `@theme` tokens and refresh shadcn components. **Owns:** `ui/src/components/**`, `ui/src/lib/**` (styles), `ui/src/index.css`. [ui] **Verify:** vitest green; no overflow or style regressions in basic smoke render. *(verified 2026-09-09: codebase already on CSS-first @theme inline oklch tokens; no hardcoded colors/!-overrides; vitest 314/314 green; browser smoke clean, Lighthouse a11y 100 light+dark)*

## 5. UI — reorganization

- [x] 5.1 Move `ui/src/pages/*` page components + route declarations into `src/features/{add,search,assets,reviews,finance,documents,docs}` (imports temporarily adjusted; shared primitives stay in `components/`); update `ui/src/router.tsx` + `src/context` wiring to compose the new paths. **Owns:** `ui/src/pages/**`, `ui/src/features/*/page component roots`, `ui/src/router.tsx`, `ui/src/context`. [ui] **Verify:** app builds + all existing vitest suites green post-move; routes unchanged. *(done 2026-09-09: 28 files git-mv'd to features/{add,assets,reviews,finance,search,documents}, 51 import fixes, pages/ deleted, routes byte-identical, vitest 312/314 baseline, ui-reviewer PASS)*
- [x] 5.2 Co-locate each feature's hooks/components/tests inside its `src/features/<feature>/` folder (imports updated, shared bits into `features/docs/**`); only `features/docs/` may be imported cross-feature per modular-structure delta.  **Owns:** `ui/src/features/**` (hooks/components/test files). [ui] **Verify:** all co-located tests pass; no feature→feature import outside `features/docs/`. *(done 2026-09-09: split lib/api/hooks.ts 812→395 into per-feature hook files (docs/assets/finance/add/search/reviews), lib slimmed to keys+plumbing+orphans+useQuickSearch (single-sourced); upload/data-table/confirm-dialog+quick-search → features/docs/; 12+ test vi.mock re-pointed; every cross-feature import targets only @/features/docs/; vitest 312/314 = baseline, tsc 0 new errors, ui-reviewer PASS)*
- [x] 5.3 Add ESLint `no-restricted-imports` rule set enforcing the cross-feature import boundary, auto-generated per feature dir. **Owns:** `ui/.eslintrc*`/`eslint.config.*`, boundary tests. [ui] **Verify:** lint reports violation on a synthetic cross-feature import and passes clean app-wide. *(done 2026-09-09: NEW eslint.config.js (flat, typescript-eslint v8) w/ auto-generated per-feature no-restricted-imports overrides scanning src/features/* at config-load; eslint.boundary.test.ts 7/7 via ESLint.lintText (synthetic cross-feature fires, docs/same-feature/docs-as-source/outside clean); eslint@9.39.5+typescript-eslint@8.70.0 installed, "lint":"eslint ." script; fixed 1 pre-existing prefer-const; npm run lint exit 0/0 errors/0 boundary violations app-wide; vitest 319/321 = 2 pre-existing codegen-drift (10.1 scope); ui-reviewer PASS 0crit/0major/3minor all fixed)*

## 6. UI — dark mode

- [x] 6.1 Add theme provider (system default, localStorage persist, live `prefers-color-scheme` listener) + `class="dark"` strategy applied before first paint; add a theme toggle control (light/dark/system) reachable on app chrome and landing page; map Tailwind v4 tokens. **Owns:** `ui/src/context/theme-provider*`, toggle component file(s), theme tokens css. [ui] **Verify:** vitest — first-token applied pre-paint, persistence across reload, system-follow updates live; default is system.

## 7. UI — hamburger nav + landing page + ingest hub

- [x] 7.1 Build the shared hamburger navigation: ☰ trigger fixed top-right on **every view** (landing included) opening a slide-in sheet overlaying content from the right — profile row (circular avatar + user name, opens profile/active-user context), nav links Review Queue / Assets / Documents / Accounts, and disabled TBD placeholder items (Subscriptions / Relationships / Inventory, inert, visible); dismiss via scrim/✕/link tap with focus returning to the trigger. No sidebar/sheet-side rail on any route — this is the only nav mechanism. **Owns:** `ui/src/components/nav-sheet/**`, `ui/src/components/AppShell*` (top-bar trigger). [ui] **Verify:** vitest: ☰ opens sheet on `/` and on `/assets`; sheet lists profile + 4 nav links + 3 disabled TBD items; link tap navigates + dismisses; scrim dismiss keeps route and restores focus.
- [x] 7.2 Build `ui/src/features/landing/landing-page.tsx` (route `/` rendered directly, no redirect): search bar, centered [+] button, Insights labeled placeholder, fixed disabled chat pill; mobile-first 360px layout per spec sketch (cards stack vertically at 360px, 3-up ≥1024px); consumes the shared hamburger sheet (7.1). **No persistent navigation anywhere.** **Owns:** `ui/src/features/landing/landing-page.tsx`, `landing-page.test.tsx`, `ui/src/router.tsx`. [ui] **Verify:** vitest: search navigates to `/search?q=`; `/` renders with no sidebar; hamburger visible top-right.
- [x] 7.3 Build the [+] ingest cards + wire ingest flows to the updated API: three option cards — Camera/Image (capture/pick via getUserMedia or file input), Document (pick PDF/allowed format), Text (own text entry, no file required) — each with an inline optional note field ("Add a note about this…") passed with the ingestion as a user directive; no pre-selection modal, no separate form step; duplicate-prompt modal rendered from the `DuplicateReport.prompt` fields (`reprocess_uri`/`keep_uri`, `expires_at`, verbatim `timeout_toast`) with reprocess/keep choices. **Owns:** `ui/src/features/landing/cards/**`, `ui/src/features/add/**`. [ui] **Verify:** vitest: card reveal per mode; note travels as directive; text-only ingest works; both dup prompt choices; keep-default on timeout (toast text matches `timeout_toast`). *(done 2026-09-09: cards + dialog were ~complete on disk from a prev worker (uncommitted) but had a time-bomb test fixture; fixed `DUPLICATE_REPORT.expires_at` past→now+10min (3 dup-choice tests green), reworded stale landing [+] comment, `INGEST_PLACEHOLDER_ID`→`INGEST_CARDS_ID`, removed camera `console.debug`; vitest 71/71 target, npm test 366/2 = 2 pre-existing codegen.drift (10.1 scope), tsc 0 new errors, lint 0err/0 boundary; browser 360 1-col stack + 1280 3-up no-overflow, Lighthouse a11y 100, console clean; ui-reviewer PASS-WITH-MINORS 0crit/0major/4minor → apply-7-3-ingest-cards.md)*

## 8. UI — documents section

- [x] 8.1 Build `src/features/documents` list view: rows w/ filename/upload date, processing status badge (`processed`/`in_review`/`failed`/`asset_less`), asset link for processed rows; status filter + filename search; empty state linking back to landing [+] hub; row menu (reprocess w/ optional comment, delete w/ confirm, open asset, edit asset fields via asset update path, asset reprocess). **Owns:** `ui/src/features/documents/**`. [ui] **Verify:** vitest row/status/filter/render tests incl. processed-link case and row-menu affordances. *(done 2026-09-09: list view + 4-state badge + asset link + status filter + filename search + empty CTA + status-aware row menu (Delete / Reprocess±comment / Open asset / Edit asset); 30 vitest green (17 page+10 hooks+3 upload); "Reprocess asset" menu item deferred — no backend endpoint exists (see Blockers) → report apply-8-1-documents-view.md)*

## 9. PWA mobile-readiness

- [x] 9.1 Add `vite-plugin-pwa` (manifest name/icons/standalone display, app-shell service worker, production-build-only SW). **Owns:** `ui/vite.config.*`, `ui/public/manifest.webmanifest`, PWA icon assets dir. [ui] **Verify:** build emits SW; manifest installs correctly per installability check in the UX plan. *(done 2026-09-09: vite-plugin-pwa@1.3.0; static public/manifest.webmanifest (Procrastinator/standalone/orientation any/theme #6366f1/192+512+maskable PNGs, 4 icons incl apple-180) + index.html head links; app-shell SW in vite.config.ts (generateSW/autoUpdate, devOptions.enabled:false prod-only, manifest:false, workbox precache+navigateFallback /index.html+/api denylist+NetworkFirst /api+globIgnores mockServiceWorker.js); build emits sw.js+workbox+manifest (precache 19), all installability criteria true, offline /assets shell renders, Lighthouse a11y 100, vitest 366/2 (2 pre-existing 10.1); ui-reviewer PASS-WITH-MINORS 0crit/0major/3minor (M-1+M-3 fixed, M-2 deferred) → apply-9-1-pwa.md)*

## 10. Compiled admin + drift

- [x] 10.1 Sync `ui/src/lib/api/generated` to entity-shaped API via codegen; update all type imports across features. **Owns:** `ui/src/lib/api/generated/**`, touched feature files. [ui] **Verify:** ui type-check + vitest pass on the new shaped models. *(done 2026-09-09: generated was ALREADY in sync from task 3.1 regen (drift tests 9/9, no re-run needed); fixed the one unmigrated component features/reviews/review-queue-page.tsx (data.doc_type/confidence/state accessors + review.data.state), re-shaped 13 test-file fixtures to `{id,...,data:{...}}` (asset/acc/movement/batch/review incl. top-level source/status nuances + required data.metadata/candidate_fields), updated the reprocessDocument test to the new body/Content-Type behavior, and cleared 12 pre-existing test-typing nits (sonner mock + null-asserts + unused import); tsc -b exit 0, vitest 395/395, drift 9/9, lint 0 err, npm build exit 0; ui-reviewer PASS-WITH-MINORS 0crit/0major (a11y 100, no overflow, no console errors) → apply-10-1-ts-codegen.md)*

## 13. UI + delta-2 — app chrome & route surface (app-chrome)

- [x] 13.1 Route-surface cleanup per design D9: reduce router to `/` / `/search` / `/ingest/reviews` / `/assets` / `/assets/:assetId` / `/documents` / `/finance/accounts` + `*` not-found; legacy routes redirect to closest surviving view (`/add`→`/`; `/finance/movements`, `/finance/import`, `/finance/import/history`, `/finance/import/:batchId` → `/finance/accounts`); delete route-tree-exclusive page/flow components listed in D9; add a route-table vitest test (each reachable route renders its view; each legacy path redirects; unknown → not-found; `/assets/:id` deep-links). **Owns:** `ui/src/router.tsx`, deleted files listed in D9. [ui] **Verify:** `npx vitest run src/router.test.tsx` (new) + `npm test` green; browser walk shows no link to a removed route. *(done 2026-09-09: router → 7 reachable routes + 5 `<Navigate replace>` legacy redirects + `*` catch-all; quick-search hitRoute (movement/import_batch) + asset-list empty-state CTA re-pointed off legacy routes; 17 route-tree-exclusive files deleted (add-page, finance movements/import chain, dev-preview) + `ui/.gitignore` cleaned; NEW `ui/src/router.test.tsx` 10/10 (route-table: 7 views + 5 redirects + not-found + /assets/:id deep-link); `app-shell.test.tsx` dead deep-link tests + dead addHooks/finance movement·batch mocks removed; npm test 33/359, tsc -b 0, lint 0err; browser walk 13 route cases — no dead links, no 360px overflow, console clean; ui-reviewer PASS 0crit/0major/1minor-fixed (dup vi.mock blocks removed) → apply-13-1-route-surface.md)*
- [x] 13.2 App-shell chrome rewrite per D10: remove the `<header>` top bar entirely; hamburger becomes a fixed **top-left** floating ☰ (≥44px, arithmetic label "Open navigation") rendered on every view incl. landing; remove bar ThemeToggle/brand from the shell. **Owns:** `ui/src/components/layout/app-shell.tsx`, `app-shell.test.tsx`. [ui] **Verify:** `npx vitest run src/components/layout/app-shell.test.tsx` — no header element in snapshot, ☰ present top-left on `/` and `/assets`.
- [x] 13.3 Nav-sheet absorbs profile+Home+theme per D10: content order = profile row → **Home** link (first nav item → `/`) → Review Queue / Assets / Documents / Accounts → theme toggle row (reuse `theme-toggle`) → disabled TBD rows; focus-restore unchanged. **Owns:** `ui/src/components/nav-sheet/nav-sheet.tsx`, `nav-sheet.test.tsx`, `theme-toggle.tsx` (consumption only). [ui] **Verify:** `npx vitest run src/components/nav-sheet` — Home first, tap navigates "/" and dismisses; theme toggle present in sheet, absent from any bar. *(done 2026-09-10: Home first w/ `end` exact-match + theme toggle row (reuse `theme-toggle`) added between nav and TBD, focus-restore unchanged; nav-sheet+app-shell 34/34, eslint 0err, tsc -b clean; browser a11y tree confirms exact D10 order @ 360px+1280px; ui-reviewer PASS Lighthouse a11y 100, 1 major = pre-existing shared sheet.tsx ✕ 28px → tracked follow-up → apply-13-3-nav-sheet.md)*
- [x] 13.4 Shared top-right context [+] control: fixed top-right icon button ≥44px, per-section action labels ("Add asset" / "Add document" / "Add account" / "Add review"), opening that section's composer flow (14.x); rendered on `/ingest/reviews`, `/assets`, `/documents`, `/finance/accounts`; **not** on `/`. **Owns:** `ui/src/features/docs/add-button.tsx` (new shared), consumers untouched (wiring is per-feature in 14.x). [ui] **Verify:** `npx vitest run src/features/docs/add-button.test.tsx` — renders labeled, ≥44px target; landing renders without it.

## 14. UI + delta-2 — unified composer & intuitive input (intuitive-input / landing-page / documents-section / asset-registry)

- [x] 14.1 Shared AddComposer per D11 in `ui/src/features/docs/composer/`: describe-input + attach strip (camera/file/paste-text → removable session chips) + always-available directive note; responsive (bottom sheet narrow / inline dialog ≥`sm`); submit routes: file → `POST /api/users/{uid}/documents` (multipart `file`+`note`, 409 DuplicateReport → reuse `duplicate-prompt-dialog`), text-only → existing text ingest, camera → file path. Reuses `features/landing/cards/hooks.ts` upload plumbing + `duplicate-prompt-dialog` (delete `ingest-cards.tsx` + its tests; move hooks/dialog into the composer folder). **Owns:** `ui/src/features/docs/composer/**`, `ui/src/features/landing/cards/**` (delete/move). [ui] **Verify:** `npx vitest run src/features/docs/composer` — describe-first surface (no multi-field form), attach chips add/remove pre-submit, note travels as directive, all 3 strip actions, 409 opens the dup prompt, ≥360px no overflow.
- [x] 14.2 Landing rewrite per landing-page delta: PROCRASTINATOR hero wordmark (stylized text, Tap target → `/`, as link for AT), keep search bar / Insights / chat pill; big centered [+] opens the shared AddComposer (no 3-card grid, no corner [+], hamburger stays top-left via 13.2). **Owns:** `ui/src/features/landing/landing-page.tsx`, `landing-page.test.tsx`. [ui] **Verify:** `npx vitest run src/features/landing` — order wordmark→search→[+]→insights→pill; [+] opens composer; wordmark links `/`; no card grid remains. *(done 2026-09-10: explore-cheap audit → 14.1 already mounted AddComposer + centered [+] + search/Insights/pill; only gap = wordmark. Added `<Link to="/">` hero (L43–55, text not image, responsive tracking-[0.18em→0.3em], min-h-11 ≥44px, no 360px overflow) + five-regions-order test (compareDocumentPosition) + wordmark-link test + no-card-grid assertion; removed redundant four-regions test inline. vitest landing 8/8, npm test 35/375 green, tsc -b 0; LIVE :8080 smoke @360/1280 a11y-tree order exact + href=/ 48px + [+] opens AddComposer + console 0err; Lighthouse a11y 100; ui-reviewer PASS 0crit/0major/3minor non-blocking (M1 font-metric tracking deferred, M2 two same-purpose / links weight-0 obs, M3 pre-existing SEO) → apply-14-2-landing-hero.md)*
- [x] 14.3 Review-chips progressive-disclosure component per D11 in `ui/src/features/docs/composer/review-chips.tsx`: extraction-output chips (confirm / tap-to-reveal+correct), collapsed "add details" affordance for unfound optional fields, save-unedited path; used by every creation flow post-submit. **Owns:** `ui/src/features/docs/composer/review-chips.tsx` + test. [ui] **Verify:** `npx vitest run review-chips` — chips confirm/correct, tap-to-reveal, collapsed details; no empty field grid.
- [x] 14.4 Asset composer flow: `/assets` corner [+] → AddComposer → review chips → create through the existing asset-create endpoint (payload values exactly as confirmed chips; duplicate-serial path applies unchanged); delete/demote any legacy asset-create form entry. **Owns:** `ui/src/features/assets/asset-list-page.tsx` (wiring), new `asset-composer-flow.tsx`, its test. [ui] **Verify:** `npx vitest run src/features/assets` — first interaction is one describe-input (no brand/model/serial empty form), chip confirm→POST asset body matches chip values.
- [x] 14.5 Accounts + reviews composer flows: `/finance/accounts` and `/ingest/reviews` corner [+] open the same composer/chip pattern (account fields / review ingest respectively) through existing create endpoints; legacy form entry points demoted to edit-time only. **Owns:** `ui/src/features/finance/accounts-page.tsx`, `ui/src/features/reviews/review-queue-page.tsx` (wiring + 2 new small flow files + tests). [ui] **Verify:** `npx vitest run src/features/finance src/features/reviews` — corner [+] labeled per section, composer-first, chips confirm.
- [x] 14.6 Documents view corner [+] → shared AddComposer (upload path unchanged, 409 dup flow intact); base list contract (8.1) untouched otherwise. **Owns:** `ui/src/features/documents/documents-page.tsx` + test additions. [ui] **Verify:** `npx vitest run src/features/documents` — [+] opens composer; submitted duplicate triggers the reprocess/keep prompt; list/filter/reprocess/delete tests still green.
- [x] 14.7 [backend] Composer callsite verification per D11: confirm no OpenAPI/entity changes are needed — note→`data.user_directive`, `409 DuplicateReport` contract, no pre-selection params, asset/account create endpoints accept the composer-derived payloads — and extend the wire-parity/lockstep tests only if a gap is found. **Owns:** `procrastinator-backend/api` tests only (no contract change expected). [backend] **Verify:** `go test ./api -run "TestOpenAPI|TestWireParity"` PASS unchanged; add a test pinning note-field-only multipart acceptance if not already covered.

## 15. Delta-2 — UX audit matrix sync

- [x] 15.1 Update `ux-audit-plan.md` for delta 2: add rows for route walk (legacy→redirect/404, no dead links, `/assets/:id` deep-link), chrome (no top bar, top-left ☰, sheet Home+profile+theme), context [+] per section, unified composer (landing + documents + section flows, 3 attach actions, chips), review-chips pattern; explicitly mark carried-over rows (dup-prompt choices, documents actions, finance, reviews, dark mode) — supersedes card-grid rows. **Owns:** `openspec/changes/asset-management-v2/ux-audit-plan.md`. [ui] **Verify:** matrix spot-read: every delta-2 scenario from `app-chrome`/`intuitive-input`/`landing-page`/`ux-test-plan` maps to ≥1 row; carried-over rows present.

## 11. UX test plan (apply gate)

- [x] 11.1 Enumerate the full UX audit matrix per design D8 (flow × viewport × mode) into a checklist file, marking which rows are automated (vitest/browser checks) vs manual-with-screenshots. Covers: mobile 360×640 + desktop ≥1280 (768 optional) × landing, hamburger ☰ sheet open/close + each nav link lands its view (incl. disabled TBD items), [+] ingest cards (Camera/Image, Document, Text — with + without inline note), search flow, asset detail/edit, documents (list, filter, reprocess+comment, delete, edit-asset, asset-reprocess), finance, reviews queue accept/discard, duplicate-prompt both choices + timeout, dark mode on every flow. **Owns:** `openspec/changes/asset-management-v2/ux-audit-plan.md` (new). [ui] **Verify:** every matrix row present; quality-bar criteria present; gate wired before apply signoff.
- [x] 11.2 Execute the UX audit: run automated rows vitest/browser and manually trace manual rows at 360px + desktop in light/dark, capturing screenshots; record pass/fail next to each row. **Owns:** execution + screenshots stored in `.autopilot/change-asset-management-v2/ux-evidence/`. [ui] **Verify:** rows are all pass/fail-tagged with visible evidence paths; failures have rows to show → next iteration retry.
- [x] 11.3 Fix any UX failures (each failing row) and re-verify rows to green until the quality bar holds (no 360px horizontal overflow, ≥40px touch targets, readable contrast, no theme-shift layout jump, loading skeletons, visible keyboard focus, toasts on destructive-adjacent actions). **Owns:** failing-rows files re-owned by the fixer. [ui] **Verify:** re-executed rows all pass; signoff recorded in the checklist. *(done 2026-09-11: PART 2 reported 0 FAIL (63/63 browser rows PASS; F7f DEFERRED/no-endpoint excluded) → 11.3 = light re-verification; `npx vitest run` full suite green 426/426 (39/39 files) = PART 1 baseline, no new failures from 14.x edits; 0 failing rows, nothing to fix → apply-11-2-ux-execute.md §11.3)*

## 12. Signoff

- [x] 12.1 Full backend + UI test pass and mark change applied per UX test-plan gate. **Owns:** `openspec/changes/asset-management-v2/tasks.md` status updates. [ui] **Verify:** `go test ./...` and vitest green on the final tree; UX checklist fully green; apply unblocked. *(DONE 2026-09-11 — final gate PASS 0crit/0major. gates re-verified fresh: vitest 432/432, lint 0err, tsc -b exit 0, build pass, go build/vet/test ok. cycle-1 CRIT-D2-01 (multi-file composer ingest) + MAJ-D2-01 (inert account note) both fixed + verified in diff. cycle-3 FINAL gate: CRIT-C-01/02/MAJ-C-01 resolved (icons now committed, CRIT-C-01 = false-positive dirty-worktree). committed b0188dc/72f7a7f. tasks.md 12.1 checked)*
