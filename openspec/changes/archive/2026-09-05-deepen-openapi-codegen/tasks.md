# Tasks: deepen-openapi-codegen

Each task is independently implementable and names the file(s) it owns and the spec scenario(s) it verifies (scenarios are in `specs/api-contract/spec.md`). Backend work (1–5) is complete and green before UI work (6–9) begins; pipeline work (10–11) lands with each generator.

## 1. Regenerate the strict chi server (foundation for R1 + R2)

- [x] 1.1 Update the pinned server-gen flag to `types,chi-server,strict-server` and regenerate + commit `procrastinator-backend/api/gen/openapi.gen.go`. **Owns:** `Makefile` (root) `OAPI_CODEGEN_GENERATE`, `procrastinator-backend/api/gen.go` (`go:generate` line), `procrastinator-backend/api/gen/openapi.gen.go`. Verify the output now contains `StrictServerInterface`, the per-op `*RequestObject`/`*ResponseObject` types, `NewStrictHandlerWithOptions`, and `StrictHTTPServerOptions`, while still emitting the loose `ServerInterface` + chi router. (Enables "Handlers consume generated request types with unchanged outputs", "Generated response objects produce the documented wire output".)
- [x] 1.2 Update the Go generator-invocation flags in the existing tests to `types,chi-server,strict-server` so they regenerate the same artifact. **Owns:** `procrastinator-backend/api/gen/drift_test.go`, `codegen_test.go`, `budget_test.go`. Verify the scaffolding + drift tests still pass (loose `ServerInterface` is still emitted). (Supports "Deepened generation is deterministic".)

## 2. Centralized encoder + error mapping (R2)

- [x] 2.1 Add a typed `*apiError{status, msg}` and pure domain→(status,msg) mappers `mapIngestError`, `mapLedgerError`, `mapStatementError`, `mapHouseholdError` (moved verbatim from the current `writeProcessError`/`writeFinanceError`/`writeImportError`/`writeHouseholdError`), plus a single shared function that writes the `{"error": msg}` envelope with a status + `Content-Type: application/json`. **Owns:** `procrastinator-backend/api/errors.go` (new), `procrastinator-backend/api/httpx/json.go` (shared envelope writer; delete `WriteJSON`/`WriteError` once unreferenced). Verifies "Error envelope emitted by the error encoder", "Domain error mapping is preserved".
- [x] 2.2 Rewrite the chi `UserMiddleware`'s pre-handler 400/404/500 responses to emit the `{"error": string}` envelope via the shared writer (behavior unchanged). **Owns:** `procrastinator-backend/api/httpx/user.go` (+ `user_test.go`). Verifies "Error envelope emitted by the error encoder" (envelope shape preserved for pre-handler failures).

## 3. Server request binding (R1)

- [x] 3.1 Add a chi-level size-limit middleware that wraps `r.Body` with `http.MaxBytesReader` for `POST /api/users/{userId}/documents` (`maxBytes`) and `POST /api/users/{userId}/finance/import-batches` (`maxStatementBytes`), preserving the 413 path. **Owns:** `procrastinator-backend/api/httpx/middleware.go` (new) (+ test). Verifies the 413 half of "Missing required file yields 400"/upload behavior (oversized → 413 `{"error":...}`).
- [x] 3.2 Rewrite the JSON-body handlers to consume `request.Body` (typed) and return 400 on a nil/invalid body, dropping `json.NewDecoder(r.Body)`. **Owns:** `procrastinator-backend/api/finance_accounts.go` (`CreateAccount`), `finance_movements.go` (`CreateMovement`, `PatchMovement`, `LinkMovement`), `households.go` (`CreateHousehold`, `AddHouseholdMember`). Verifies "JSON body is bound to the generated type", "Malformed JSON body yields 400".
- [x] 3.3 Rewrite the query-param handler to consume `request.Params` (typed), dropping manual query reads. **Owns:** `procrastinator-backend/api/finance_movements.go` (`ListMovements`). Verifies "Query parameters are bound to the generated params type".
- [x] 3.4 Rewrite the two multipart handlers to bind `request.Body` (`*multipart.Reader`) via `runtime.BindMultipart` into `gen.UploadDocumentMultipartBody` / `gen.CreateImportBatchMultipartBody`; map a `*http.MaxBytesError` to 413 and a missing/empty file to 400. For `UploadDocument` derive the file MIME from the filename (`mime.TypeByExtension`) for the ingest call. **Owns:** `procrastinator-backend/api/handlers.go` (`UploadDocument`), `finance_import.go` (`CreateImportBatch`). Verifies "Multipart upload binds to the generated body type", "Missing required file yields 400", and the 413 mapping.

## 4. Server response encoding (R2)

- [x] 4.1 Rewrite every handler to the strict signature `(ctx, OpIDRequestObject) (OpIDResponseObject, error)`: return the generated success object (`gen.<Op><code>JSONResponse` / `gen.<Op>204Response{}`) and `(nil, *apiError)` for failures; remove all per-handler `httpx.WriteJSON`/`httpx.WriteError`/`w.Write`/`w.WriteHeader`. **Owns:** `procrastinator-backend/api/handlers.go`, `finance_accounts.go`, `finance_movements.go`, `finance_import.go`, `households.go` (+ `dto.go`/`finance_dto.go` converters unchanged). Verifies "Success body emitted by the encoder", "No-content operation returns 204 with no body", "Generated response objects produce the documented wire output".
- [x] 4.2 Re-wire `server.go`: `var _ gen.StrictServerInterface = (*Server)(nil)`; `Routes()` = `gen.HandlerWithOptions(gen.NewStrictHandlerWithOptions(s, nil, gen.StrictHTTPServerOptions{RequestErrorHandlerFunc, ResponseErrorHandlerFunc}), gen.ChiServerOptions{BaseRouter, Middlewares:[UserMiddleware, MaxBodyMiddleware]})`; implement the two centralized error funcs (request: malformed→400, `*http.MaxBytesError`→413; response: unwrap `*apiError`→envelope, else 500). **Owns:** `procrastinator-backend/api/server.go`. Verifies "Error envelope emitted by the error encoder", "Domain error mapping is preserved".

## 5. Wire-parity + binding verification (R1 + R2)

- [x] 5.1 Add/extend handler tests asserting the observable contract: valid JSON/multipart/query inputs yield the same status + body as before, and the handler signatures accept the generated request types (no hand-parsing). **Owns:** `procrastinator-backend/api/handlers_test.go`, `finance_movements_test.go`, `finance_accounts_test.go`, `finance_import_test.go`, `households_test.go`. Verifies "Handlers consume generated request types with unchanged outputs", "JSON body is bound to the generated type", "Malformed JSON body yields 400", "Multipart upload binds to the generated body type", "Query parameters are bound to the generated params type".
- [x] 5.2 Add a wire-parity test that issues the same set of documented requests (success + each documented non-2xx + a 204) and asserts status, `Content-Type`, and body are byte-identical to the recorded pre-change outputs. **Owns:** `procrastinator-backend/api/wire_parity_test.go` (new, with recorded fixtures in `api/testdata/`). Verifies "Wire parity before and after", "Generated response objects produce the documented wire output", "No-content operation returns 204 with no body".

## 6. Client: orval per-operation generation (R3)

- [x] 6.1 Add a pinned orval devDependency + committed `ui/orval.config.ts` (input `procrastinator-backend/api/openapi.yaml`, output `ui/src/lib/api/generated/client`, fetch-mode per-operation client) and generate the client. **Owns:** `ui/package.json`, `ui/orval.config.ts`, `ui/src/lib/api/generated/client/*` (new). Verifies "Per-operation typed function per documented operation", "Contract field flows to the client".

## 7. Client: `ApiError` mutator + error/network parity (R3)

- [x] 7.1 Implement the orval mutator that wraps `fetch` to reproduce `apiFetch`'s contract: `!ok` → `ApiError(status, errorCopy(status), errorDetailFromBody(...))`; thrown/timeout fetch → `ApiError(NETWORK_STATUS, errorCopy(NETWORK_STATUS))` (status `0`). **Owns:** `ui/src/lib/api/client.ts` (mutator), `ui/src/lib/api/errors.ts` (unchanged API, reused). Verifies "Error handling preserved", "Network failure surfaces the same error".

## 8. Client: re-point the UI transport (R3)

- [x] 8.1 Remove `apiJson`/`apiVoid` + the `Resource`/`PathFor`/`buildUrl` string bridge from `client.ts`; re-point `hooks.ts` `queryFn`/`mutationFn` to the generated per-operation functions, keeping the query keys + D5 invalidation map unchanged; adapt `schema.ts` re-exports to the generated types. **Owns:** `ui/src/lib/api/client.ts`, `hooks.ts`, `schema.ts`. Verifies "Read calls route through the typed client", "Write calls use a typed request body", "UI calls route through typed per-operation functions".
- [x] 8.2 Keep the XHR `performUpload` for the two upload operations (progress preserved), typed against the generated multipart request/response types and routing URL/body through the generated client config. **Owns:** `ui/src/lib/api/upload.ts` (+ `upload.test.ts`). Verifies "Multipart upload is typed and reports progress".
- [x] 8.3 Extend UI tests to assert emitted HTTP requests (method/path/body) + parsed responses match the pre-change behavior, and that there are no `any`/untyped bodies. **Owns:** `ui/src/lib/api/client.test.ts`, `hooks.ts` tests, `upload.test.ts`. Verifies "UI calls route through typed per-operation functions", "Read calls route through the typed client", "Write calls use a typed request body".

## 9. Pipeline: pin, drift-check, budget the new generators (R4)

- [x] 9.1 Extend the root `Makefile` `codegen` to include the orval client target and `codegen-drift-check`/`budget-check` to cover the client artifact. **Owns:** `Makefile` (root). Verifies "Full deepened codegen is within the time budget", "Stale per-operation client fails the drift check".
- [x] 9.2 Extend the Go budget test to include the strict server gen in the timed full-codegen + drift runs (still < 30s each) and re-confirm doc < 200 KB. **Owns:** `procrastinator-backend/api/gen/budget_test.go`. Verifies "Full deepened codegen is within the time budget", "Deepened generation is deterministic", "OpenAPI document is within the size budget".
- [x] 9.3 Add a UI orval drift test (re-run orval, diff the committed client; fail naming the stale artifact) + a UI budget assertion (full codegen incl. orval < 30s). **Owns:** `ui/src/lib/api/codegen.drift.test.ts` (extend) / `ui/src/lib/api/codegen.client.drift.test.ts` (new). Verifies "Stale per-operation client fails the drift check", "Re-running codegen clears drift", "Full deepened codegen is within the time budget".
